.claude/*
!.claude/CLAUDE.md

# Build output and environment secrets (added 01-01, ahead of the Astro
# scaffold in 01-02, so npm install never has a window to commit either)
node_modules/
dist/
.astro/
.env
.env.*
!.env.example

# Throwaway fallback-scaffold staging directory (01-02 Task 2). Ignored
# before the fallback scaffold ever runs, so a failed cleanup cannot sweep
# it into the first site commit.
.astro-scaffold-tmp/
