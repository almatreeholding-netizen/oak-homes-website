---
phase: quick
plan: 260901-t59
subsystem: ui
tags: [astro, css, a11y, wcag-contrast, sharp]

# Dependency graph
requires:
  - phase: 02-publishing (02-01)
    provides: CMS admin shell, Netlify deploy config, Homes collection
provides:
  - Full-bleed photographic hero banner on the homepage
  - A computed (pixel-level) WCAG AA contrast gate (`hero-contrast`) in the verification CLI
affects: []

# Actuals (#2632) — pairs with the plan's `estimate` to calibrate future estimates.
actuals:
  tokens: 2793
  tasks: 3
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Computed (not asserted) AA contrast gates: composite the real overlay CSS over real decoded JPEG pixels via sharp().raw(), never assert a pre-computed number"
    - "Full-bleed section pattern: drop max-width/margin from the outer <section>, move them to an inner wrapper div, relying on Layout.astro's unconstrained <main>"

key-files:
  created: []
  modified:
    - public/uploads/hero/home-hero.jpg
    - scripts/verify/checks.mjs
    - src/pages/index.astro

key-decisions:
  - "Overlay alphas set to 0.62/0.72, not the mockup's 0.55/0.66 -- the mockup's overlay measurably fails WCAG AA (3.913:1) against the real photo's blown-out sky pixels; 0.62/0.72 clears 4.5:1 with margin (plan design decision D-B)"
  - "hero-contrast reads its colour tokens from src/styles/global.css at check-runtime rather than hardcoding hex values, coupling the gate to the design tokens so a token change is caught automatically"

patterns-established:
  - "Pixel-level AA gates for any future full-bleed/overlay-text section should follow hero-contrast's shape: parse tokens from global.css, parse overlay literals from source, decode the real asset with sharp, composite, and fail with the exact ratio/location"

requirements-completed: [QUICK-260901-t59]

coverage:
  - id: D1
    description: "Hero JPEG committed byte-identical and a computed hero-contrast AA gate added to scripts/verify/checks.mjs, failing (RED-AS-EXPECTED) before the hero markup existed"
    requirement: "QUICK-260901-t59"
    verification:
      - kind: other
        ref: "node scripts/verify/checks.mjs hero-contrast (pre-Task-2, exit 1, FAIL naming missing hero declaration)"
        status: pass
      - kind: other
        ref: "node scripts/verify/checks.mjs bogus-id (confirms hero-contrast listed in Known checks)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Homepage hero reworked into a full-bleed photographic banner: accent pill, unchanged h1, CMS-bound intro, button, dark overlay, cream text, all colours via var(--color-*) tokens except the two overlay rgba() stops"
    requirement: "QUICK-260901-t59"
    verification:
      - kind: other
        ref: "node scripts/verify/checks.mjs hero-contrast (post-Task-2, PASS: absolute 4.878:1, block 4.901:1, pill 11.934:1)"
        status: pass
      - kind: other
        ref: "git diff scoping checks: no steps-section/featured-section/StepList/PropertyCard/getCollection lines touched; no new 48/20/12px font-size; zero literal hex colour declarations"
        status: pass
    human_judgment: false
  - id: D3
    description: "Build proof and accessibility regression sweep of the committed tree in a short-path copy (C:/tmp/oak-verify), reusing the documented Rolldown/Windows-path workaround"
    verification:
      - kind: other
        ref: "cd /c/tmp/oak-verify && npm run build (exit 0, 10 Astro-rendered pages + 1 static admin/index.html = 11 total dist/*.html)"
        status: pass
      - kind: other
        ref: "node scripts/verify/checks.mjs a11y-sweep in /c/tmp/oak-verify"
        status: fail
      - kind: other
        ref: "node scripts/verify/checks.mjs hero-contrast in /c/tmp/oak-verify (PASS, same ratios as source-level run)"
        status: pass
    human_judgment: true
    rationale: "a11y-sweep hard-fails on 'expected exactly 10 built .html files, found 11'. Confirmed via a baseline build of commit b98ab61 (the last commit before this quick task's changes) that this 11-vs-10 mismatch is pre-existing and caused entirely by phase 02-01's CMS admin shell (public/admin/index.html), not by this task's hero-banner changes -- see Deviations below. A human should confirm this is an accepted pre-existing gap (already logged to .planning/WINDOWS.md) rather than something this quick task should have fixed."

# Metrics
duration: 35min
completed: 2026-09-02
status: complete
---

# Quick Task 260901-t59: Full-Bleed Hero Banner Summary

**Homepage hero rebuilt as a full-bleed golden-hour photo banner with a computed WCAG AA overlay (0.62/0.72 alpha, not the mockup's failing 0.55/0.66) and a new pixel-level `hero-contrast` gate that proves the AA claim against the real JPEG instead of asserting it.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-09-02T00:47:00Z
- **Completed:** 2026-09-02T01:22:00Z
- **Tasks:** 3 (2 committed, 1 verification-only)
- **Files modified:** 3 (`public/uploads/hero/home-hero.jpg`, `scripts/verify/checks.mjs`, `src/pages/index.astro`)

## Accomplishments
- Committed the already-staged 1500x844 hero JPEG (220566 bytes) byte-identical
- Added a `hero-contrast` check to `scripts/verify/checks.mjs` that: validates the asset shape, resolves colour tokens from `global.css` (never hardcoded), asserts the required hero source declarations, forbids any literal hex colour outside the two overlay `rgba()` stops, and computes real WCAG AA contrast ratios by decoding the JPEG's raw pixels with `sharp` and compositing the overlay over both a pure-white worst case and an 8x8 block-average worst case
- Reworked `.hero` in `src/pages/index.astro` into a full-bleed banner: dropped `max-width: 64ch`, added a `.hero-inner` 1200px container, added an accent "From Rent to Roots" pill, and applied the AA-safe two-stop overlay over the committed photo -- while leaving the h1, the CMS-bound intro, the button, and everything below the hero untouched
- Proved the change builds and does not regress accessibility by exporting the committed tree via `git archive` into a short-path copy (`C:/tmp/oak-verify`) and running a full `npm run build`, confirming the hero markup, single `<h1>`, and hero image all land correctly in `dist/`

## Task Commits

Each task was committed atomically:

1. **Task 1: Commit the hero asset and add a computed hero-contrast gate** - `5b5341f` (feat)
2. **Task 2: Rework the .hero section of index.astro into a full-bleed banner** - `3772b9f` (feat)
3. **Task 3: Prove the build and the a11y suite in a short-path copy** - no commit (verification-only task; no repo files modified, as specified in the plan)

**Plan metadata:** pending (orchestrator commits SUMMARY.md/STATE.md/WINDOWS.md separately per this task's constraints)

## Files Created/Modified
- `public/uploads/hero/home-hero.jpg` - Committed byte-identical (220566 bytes, 1500x844 JPEG)
- `scripts/verify/checks.mjs` - New `hero-contrast` check (async, sharp-based pixel contrast computation)
- `src/pages/index.astro` - `.hero` reworked into a full-bleed banner (`.hero-inner`, `.hero-tag`, overlay, mobile breakpoint)

## Decisions Made
- Kept the mockup's 104px vertical hero padding and 8pt-grid pill padding (8px 16px) per plan design decisions D-D/D-E -- no new type size or spacing token introduced
- Used the *lighter* of the two parsed overlay alphas as the sole worst case for both the absolute and block contrast computations, since `background-position`/`background-size: cover` make the visible crop viewport-dependent and unpredictable -- conservative by construction

## Deviations from Plan

### Auto-fixed Issues

None - Tasks 1 and 2 executed exactly as written; no Rule 1-3 auto-fixes were needed.

### Out-of-Scope Discovery (SCOPE BOUNDARY -- logged, not fixed)

**1. `a11y-sweep`'s hardcoded "exactly 10 built .html files" assertion is stale relative to phase 02-01's CMS admin shell**
- **Found during:** Task 3 (build proof in `C:/tmp/oak-verify`)
- **Issue:** `npm run build` in the short-path copy correctly produces 10 Astro-rendered pages plus `dist/admin/index.html` (the static CMS shell added in phase 02-01, commit `e076b56`) -- 11 `.html` files total. `a11y-sweep` (written in Phase 1, before the admin shell existed) still asserts exactly 10 and fails with `expected exactly 10 built .html files, found 11`.
- **Confirmed pre-existing, unrelated to this task:** Built commit `b98ab61` (the last commit before this quick task's Task 1) in an isolated short-path copy (`C:/tmp/oak-verify-baseline`) and reproduced the identical 11-file/`a11y-sweep` failure with zero hero-banner changes present. This is not a regression introduced by 260901-t59.
- **Action taken:** Did not modify `a11y-sweep` or its page-count assertion -- that check and its "exactly 10" constant are outside this task's declared file scope (`scripts/verify/checks.mjs`'s Task 1 acceptance criteria explicitly restrict the diff to the new `hero-contrast` entry only), and the root cause (phase 02-01's admin shell) is unrelated to the hero banner. Logged to `.planning/WINDOWS.md` (`deviation` kind, id 1) for cross-phase visibility instead.
- **Mitigating evidence that no hero-specific a11y regression exists:** manually re-verified, directly against `dist/index.html` in the built copy, every structural assertion `a11y-sweep` would otherwise check for this page: `lang="en"` present, `charset="utf-8"` present, exactly one `<main>`/`<nav>`/`<footer>` landmark, skip link (`class="skip-link"` + `href="#main-content"`) present, exactly one `<h1>`, no unlabeled `<img>` (the hero photo is a CSS `background-image`, never an `<img>`, so no new alt-text surface was introduced), and zero occurrences of the accent hex/token used as a text `color:` anywhere in `src/**/*.astro`.
- **Related, smaller deviation in the same task:** the plan's acceptance criterion "exactly one `.css` file under `dist/` contains `/uploads/hero/home-hero.jpg`" does not hold literally -- Astro's default `inlineStylesheets: 'auto'` inlined `index.astro`'s small page-scoped `<style>` directly into `dist/index.html` rather than emitting a separate `.css` chunk (the shared `Layout.BB83UPGa.css` chunk, which does not contain hero styles, is the only `.css` file that exists). The underlying intent -- exactly one built artifact carries the reference, and Lightning CSS processed/minified it (confirmed: `rgba(26, 24, 20, 0.62)` became `#1a18149e` in the built output) -- holds; only the artifact type (inline `<style>` vs. separate `.css` file) differs from the plan's assumption.

---

**Total deviations:** 0 auto-fixed; 1 out-of-scope discovery logged (not fixed, per SCOPE BOUNDARY) plus 1 related build-artifact-shape observation.
**Impact on plan:** No impact on the hero banner's correctness or AA compliance -- both are proven independently by `hero-contrast`, which passes both at the source level and against the built copy. The logged item is a pre-existing gap in an unrelated, already-shipped check that a future phase/quick-task should close by updating `a11y-sweep`'s expected page count (or excluding `admin/index.html` from its sweep).

## Issues Encountered
- `npm run build` and `astro dev` cannot run directly inside this worktree's deeply-nested path (`Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)` from `@rolldown/binding-win32-x64-msvc`) -- pre-existing, documented in `02-01-SUMMARY.md`, worked around per the plan via the `git archive` + short-path-copy technique in Task 3. Not caused by this task.
- `a11y-sweep` fails in the short-path copy for the pre-existing, unrelated reason documented above under Deviations.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- The homepage hero is complete, AA-compliant (proven, not asserted), and does not disturb `steps-section`/`featured-section`.
- `.planning/WINDOWS.md` now carries one open `deviation` entry (id 1) for the stale `a11y-sweep` page count -- a future phase or quick task should update `a11y-sweep` to expect 11 pages (or to explicitly exclude `dist/admin/index.html` from its sweep, since the CMS shell is not an Astro-rendered content page) and close that ledger entry.

---
*Phase: quick*
*Completed: 2026-09-02*

## Self-Check: PASSED

- FOUND: public/uploads/hero/home-hero.jpg
- FOUND: scripts/verify/checks.mjs
- FOUND: src/pages/index.astro
- FOUND: .planning/quick/260901-t59-add-full-bleed-hero-banner-image-to-home/260901-t59-SUMMARY.md
- FOUND: commit 5b5341f
- FOUND: commit 3772b9f
