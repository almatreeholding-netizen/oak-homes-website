---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - scripts/extract-mockup-photos.mjs
  - public/uploads/properties/614-e-marengo-st/
  - public/uploads/properties/2734-brown-st/
  - src/content/properties/614-e-marengo-st.md
  - src/content/properties/2734-brown-st.md
  - src/pages/homes/index.astro
  - src/pages/homes/[slug].astro
  - src/components/PropertyCard.astro
  - src/components/StatusBadge.astro
  - src/components/Gallery.astro
  - src/components/gallery-lightbox.ts
  - package.json
autonomous: true
requirements: [INFRA-04, BROWSE-01, BROWSE-02, PROP-01, PROP-02]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- INFRA-04: both real homes migrated ---
    - "Both real homes exist as content files: 614 E Marengo St ($3,000 down / $950 monthly, 6 photos) and 2734 Brown Street ($3,000 down / $1,250 monthly, 5 photos), with terms, descriptions, and feature bullets matching the mockup's window.PROPERTIES data."
    - "Eleven property photos exist as real image files under public/uploads/properties/<slug>/, extracted from the mockup's embedded base64 data — 6 for Marengo and 5 for Brown Street, in the order the mockup lists them."
    - "The Brown Street slug is 2734-brown-st, matching the house number confirmed by the owner (D-14) and corroborated by the mockup's own address field."
    # --- INFRA-04 edges (spec-less probe fallback, resolved explicit) ---
    - "INFRA-04/boundary: every committed property photo has a longest edge of at most 2000px, and a source image already at or below 2000px is written through without being upscaled."
    - "INFRA-04/precision: resizing preserves each photo's source aspect ratio to within one pixel, and output dimensions are whole integers — no photo is stretched or letterboxed by the resize."
    # --- BROWSE-01: the grid ---
    - "A visitor at /homes sees one card per home showing cover photo, address, status badge, down payment, and monthly payment."
    - "Cards are sorted Available first, then Pending, then Sold, so Sold homes stay visible at the bottom as social proof rather than being hidden (D-05)."
    # --- BROWSE-01 edges (spec-less probe fallback, resolved explicit) ---
    - "BROWSE-01/adjacency: two homes sharing the same status each render their own distinct card — equal status never merges, dedupes, or collapses two listings into one."
    - "BROWSE-01/empty: a properties collection with zero entries renders the empty-state heading 'No Homes Available Right Now' and its phone-CTA body copy, not a blank page or a broken grid."
    - "BROWSE-01/ordering: within a status group the order is deterministic and stable across rebuilds — newest publishDate first, with slug ascending as the final tiebreak, so two homes with identical status and date never swap positions between builds."
    # --- PROP-01 / PROP-02: the property page ---
    - "A property page shows an ordered gallery whose first photo is the cover, and clicking a photo opens a full-screen lightbox with previous/next controls that closes on Escape (D-08)."
    - "A property page shows down payment, monthly payment, beds, baths, square footage, the description, and the feature bullets."
    - "Any absent bed, bath, or square-footage value renders 'Call for details' rather than a blank, a dash, or a zero — 614 E Marengo St shows it for all three, and 2734 Brown Street shows 4 beds, 1 bath, and 'Call for details' for square footage (D-10)."
    - "An Available or Pending home shows the 'Inquire About This Home' call to action; a Sold home shows 'See Available Homes' linking to /homes instead, with no Inquire button present (D-09)."
    # --- UI-SPEC ## UI Considerations, surfaces E1 grid / E2 card / E3 gallery / E5 badge ---
    - "UI-SPEC loading/E1,E2,E5: grid, cards, and badges are static pre-rendered HTML containing no spinners, no skeletons, and no runtime fetch code."
    - "UI-SPEC loading/E3: gallery images carry explicit width and height attributes reserving layout space so nothing shifts as they load, and images below the fold use native lazy loading — no custom skeleton is built."
    - "UI-SPEC error/E1,E2,E5: no runtime fetch means no visitor-facing error state; a malformed content file fails the build instead."
    - "UI-SPEC empty/E1: zero listings renders the Copywriting Contract's empty-state heading and body."
    - "UI-SPEC empty/E2: a card cannot render data-less — address, price, and status are required schema fields that fail the build if absent, and optional fields fall back to 'Call for details'."
    - "UI-SPEC empty/E5: status is a required enum validated at build time, so every card and page always has a badge value."
    - "UI-SPEC populated/E1: the grid sorts Available before Pending before Sold with 24px card gaps and responsive wrapping on the 8pt scale."
    - "UI-SPEC populated/E2: card anatomy is photo, address at Label size, beds/baths/sqft meta, price figures at Heading size in price gold, and the status badge."
    - "UI-SPEC populated/E3: the gallery's lightbox previous and next controls have 44px touch targets."
    - "UI-SPEC populated/E5: exactly three badge variants render with the pinned values — Available on accent #FFD053, Pending on #D9B36C, Sold on ink #1A1A1A with cream text."
    - "UI-SPEC partial/E1,E2: a missing bed, bath, or square-footage value renders 'Call for details' per field, so a card never looks visually incomplete."
    - "UI-SPEC partial/E5: the badge enum is validated at build time, so no partial badge state is reachable."
    - "UI-SPEC overflow/E1: the grid wraps responsively with no fixed maximum card count and no horizontal scroll."
    - "UI-SPEC overflow/E5: badge copy is fixed and short with 8pt-scale padding, so its content cannot exceed its container."
    - "UI-SPEC zero-one-many/E1: the layout reads correctly at one, two, or many cards, and the D-05 sort order holds regardless of count."
    - "UI-SPEC zero-one-many/E2,E5: card and badge count behaviour is owned by the grid, and exactly one badge renders per card and per property page."
    - "UI-SPEC long-text/E3: the gallery renders no visible text besides its controls; alt text is non-visual in the normal state."
    - "UI-SPEC long-text/E5: badge copy is fixed at build time, so no truncation strategy is needed."
    - { statement: "UI-SPEC error/E3: if a photo file fails to resolve, the browser's native alt-text fallback displays (alt is required per DESIGN-04); no custom broken-image UI ships in v1 — verify visually against a deliberately missing file once the gallery exists.", verification: backstop }
    - { statement: "UI-SPEC empty/E3: a zero-photo property renders one branded placeholder frame (cream background plus the dual-leaf mark) instead of a broken layout — untested against a real zero-photo entry, since both migrated homes have photos (D-03); verify visually if content ever ships without photos.", verification: backstop }
    - { statement: "UI-SPEC overflow/E2: card text wraps within the card without clipping, and the photo crops via a fixed aspect-ratio container — verify visually with the longest migrated address at mobile width.", verification: backstop }
    - { statement: "UI-SPEC overflow/E3: no maximum photo count is set; verify the gallery thumbnail layout remains usable at high photo counts (20 or more) before adding a photo-heavy listing.", verification: backstop }
    - { statement: "UI-SPEC long-text/E1,E2: long address lines wrap to a second line at Label size without breaking card alignment — verify visually with the longest real address at 375px width.", verification: backstop }
  artifacts:
    - path: "scripts/extract-mockup-photos.mjs"
      provides: "One-off extraction of the mockup's embedded base64 photos, resized to at most 2000px before they can ever be committed oversized"
      contains: "sharp"
    - path: "src/content/properties/2734-brown-st.md"
      provides: "The second real home with owner-confirmed house number 2734"
      contains: "2734 Brown Street"
    - path: "src/pages/homes/index.astro"
      provides: "The Browse Homes grid with status sorting and the empty state"
      contains: "getCollection"
    - path: "src/components/PropertyCard.astro"
      provides: "Card rendering shared by the grid and the homepage featured section"
    - path: "src/components/StatusBadge.astro"
      provides: "The three pinned badge variants"
    - path: "src/components/Gallery.astro"
      provides: "Cover photo plus thumbnail strip, and the zero-photo placeholder frame"
    - path: "src/components/gallery-lightbox.ts"
      provides: "The full-screen lightbox island — one of only two pieces of client JavaScript in the project"
    - path: "public/uploads/properties/614-e-marengo-st/"
      provides: "6 extracted, pre-resized photos"
    - path: "public/uploads/properties/2734-brown-st/"
      provides: "5 extracted, pre-resized photos"
  key_links:
    - from: "src/pages/homes/index.astro"
      to: "src/components/PropertyCard.astro"
      via: "maps over the sorted properties collection, one card per entry"
      pattern: "PropertyCard"
    - from: "src/components/Gallery.astro"
      to: "src/components/gallery-lightbox.ts"
      via: "client:visible hydration directive — the island loads only when the gallery is scrolled to"
      pattern: "client:visible"
    - from: "src/content/properties/2734-brown-st.md"
      to: "public/uploads/properties/2734-brown-st/"
      via: "photos array holding public-root-relative paths to the extracted files"
      pattern: "/uploads/properties/2734-brown-st/"
  prohibitions:
    - statement: "MUST NOT present the down payment and monthly payment as the visitor's complete cost of ownership, and MUST NOT omit that the full terms — including what happens if payments are not made — are set out in the written agreement. A price pair shown with no such qualification reads as a total obligation, which is exactly the impression the refined legal wording exists to prevent."
      category: transparency
      status: unverified
      verification: judgment
      flagged: true
    - statement: "MUST NOT include Fair-Housing-steering language in any property description, feature bullet, or card copy — no stated or implied preference, limitation, or discrimination based on race, colour, religion, sex, familial status, national origin, or disability. Phrases describing who a home suits, or characterising a neighbourhood's people, are steering even when well-intentioned."
      category: fairness
      status: unverified
      verification: judgment
      flagged: true
    - statement: "MUST NOT present a Sold or Pending home in a way a visitor could mistake for currently available. The badge, the card treatment, and the replaced call to action must together make unavailability unambiguous — Sold listings are retained as social proof (D-05), and that purpose fails if they read as purchasable."
      category: transparency
      status: unverified
      verification: judgment
      flagged: true

flagged_assumptions:
  - id: "EDGE-PROP-01"
    source: "edge-coverage.json (spec-less probe fallback)"
    status: "unresolved"
    note: "Probe returned `unclassified — review manually`. Planner assumption: the meaningful edges for an ordered gallery are photo-count boundaries (zero and very many), and both are already carried as UI-SPEC backstops (empty/E3 and overflow/E3) plus the explicit first-photo-is-cover truth. Not silently dropped."
  - id: "EDGE-PROP-02"
    source: "edge-coverage.json (spec-less probe fallback)"
    status: "unresolved"
    note: "Probe returned `unclassified — review manually`. Planner assumption: the meaningful edge for specs and terms is field absence, which D-10's 'Call for details' fallback resolves explicitly and which both real homes exercise (Marengo is missing all three optional specs; Brown Street is missing square footage). Currency formatting precision is covered by the money-rendering acceptance criterion. Not silently dropped."
---

<!-- planner-discipline-allow: Inquire About This Home -->
<!-- planner-region-allow: Inquire About This Home -->

> **Allowlist note.** `Inquire About This Home` is asserted **present** on Available and Pending
> property pages and **absent** on a Sold one (D-09). The same literal is therefore both a positive
> and a negative gate depending on the entry's status, and it must appear verbatim in the task action
> because it is exact required copy from the UI-SPEC Copywriting Contract.

<objective>
Turn the skeleton into a real listing site: both actual Oak Homes properties, with their actual
photos, browsable as a grid and openable as full property pages with a working gallery.

Purpose: This is the phase's core user-visible capability and the first point at which the site is
worth showing anyone. It is also where the one genuinely irreversible content decision lands — the
eleven property photos are extracted from the mockup's embedded base64 and committed, and git
retains every committed blob forever. They are resized before `git add`, never after.

Output: `/homes` lists both homes; `/homes/614-e-marengo-st` and `/homes/2734-brown-st` are complete
property pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/SKELETON.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract, resize, and commit both homes' photos and content</name>
  <precondition>docs/reference/Oak-Homes-Website-SHARE.html exists in the repo and still contains its embedded base64 image data (plan 01-01 Task 1 complete).</precondition>
  <reversibility rating="costly">Git history retains every committed image blob forever. The photos are compressed mockup extractions accepted as the permanent listing photos (D-03); replacing them later works, but the originals stay in history permanently, so the resize must happen before the first `git add` rather than as a follow-up cleanup.</reversibility>
  <read_first>
    - `docs/reference/Oak-Homes-Website-SHARE.html` — read only the region around the `window.PROPERTIES` identifier (roughly lines 290-335). It holds both entries' `status`, `featured`, `name`, `address`, `city`, `state`, `down`, `monthly`, `desc`, `features`, and `photos` arrays. Do not read the whole file; it is 2.6 MB and almost entirely base64
    - `.planning/phases/01-foundation/01-RESEARCH.md` — the Code Examples section's extraction sketch, Pitfall 3 (committing full-resolution photos), Pitfall 5 (Windows' `convert.exe` shadows ImageMagick, which is not installed — use sharp), and assumption A3, which flags that a naive global regex would run across both properties' photo arrays
    - `src/content.config.ts` — the Property schema written in plan 01-02, so the frontmatter matches the validated field names and types exactly
    - `src/content/properties/614-e-marengo-st.md` — the entry created in plan 01-02, which this task completes rather than replaces
  </read_first>
  <files>scripts/extract-mockup-photos.mjs, public/uploads/properties/614-e-marengo-st/, public/uploads/properties/2734-brown-st/, src/content/properties/614-e-marengo-st.md, src/content/properties/2734-brown-st.md, package.json</files>
  <action>
Write `scripts/extract-mockup-photos.mjs` and run it once. Register it as an npm script named
`extract:photos` so re-running it is documented rather than folklore.

**Parse per property, not globally.** The mockup holds a `window.PROPERTIES` array with exactly two
entries, each containing a `photos` array of `data:image/jpeg;base64,` strings. A single global regex
over the whole file would produce one flat list of eleven images with no property boundary — the risk
RESEARCH.md records as assumption A3. Instead, locate each property object by its `address` value
(`614 E Marengo St`, then `2734 Brown Street`), and extract only the base64 strings inside that
object's own `photos` array. Assert the counts before writing anything: Marengo must yield exactly 6
and Brown Street exactly 5. If either count differs, fail loudly and write nothing — a silent
miscount would mis-assign photos between two homes, and mis-assigned real-estate photos are worse
than no photos.

**Resize before writing.** Decode each base64 payload to a buffer and pipe it through sharp with a
2000px longest-edge constraint and the no-enlargement option set, so an image already smaller than
2000px passes through at its native size rather than being upscaled into blur. Re-encode as JPEG at
quality 85. Preserve aspect ratio — do not crop, do not force a fixed height. Write to
`public/uploads/properties/<slug>/photo-01.jpg` through `photo-06.jpg`, zero-padded and numbered in
the order the mockup lists them, because the first photo is the cover (PROP-01).

Use sharp exclusively for this. ImageMagick is not installed on this machine, and Windows ships its
own unrelated `convert.exe` that shadows the name — a command that appears to succeed while producing
no image.

**Complete both content files.** Fill `src/content/properties/614-e-marengo-st.md` and create
`src/content/properties/2734-brown-st.md` from the mockup's real data:

- **614 E Marengo St** — Flint, MI. Status Available, `featured` true, `downPayment` 3000,
  `monthlyPayment` 950. Six feature bullets: bonus room, central air, full basement, detached garage,
  spacious living and dining area, covered front porch. Its description is the mockup's `desc` text.
  It supplies no beds, baths, or square footage, so leave all three unset — they render as
  call-for-details.
- **2734 Brown Street** — Flint, MI, slug `2734-brown-st`. Status Available, `featured` false,
  `downPayment` 3000, `monthlyPayment` 1250. The mockup's feature list states four bedrooms and one
  bathroom, so set `beds` to 4 and `baths` to 1 as structured fields, and keep the remaining
  descriptive bullets (updated kitchen with new cabinets, wood laminate flooring in living and dining
  areas, vinyl windows and siding, updated roof/furnace/central air, full basement with laundry in
  unit, detached two-car garage) in `features`. Do not duplicate the bedroom and bathroom counts as
  bullets once they are structured fields. Square footage is unknown, so leave `sqft` unset.

The owner-confirmed house number is 2734 (D-14), which the mockup's own `address` field
independently corroborates. Set `photos` on each entry to the public-root-relative paths of that
home's extracted files, in order. Leave `location` empty on both (D-16 — pins are verified in
Phase 3, and an unverifiable coordinate is worse than an absent one). Leave `videoUrl` and `ogImage`
unset. Set a `publishDate` on each.

Descriptions are ported from the mockup, which describes the homes rather than the people who should
live in them — keep it that way.
  </action>
  <acceptance_criteria>
    - `ls public/uploads/properties/614-e-marengo-st/*.jpg | wc -l` returns exactly 6
    - `ls public/uploads/properties/2734-brown-st/*.jpg | wc -l` returns exactly 5
    - Every extracted file is a valid JPEG whose longest edge is at most 2000px — verified by reading each file's dimensions with sharp metadata and asserting `Math.max(width, height) <= 2000`
    - No extracted image was upscaled: for each file, its dimensions are less than or equal to the decoded source buffer's dimensions
    - Each output preserves its source aspect ratio to within 1px: `abs(outW/outH - srcW/srcH) * outH < 1`
    - `find public/uploads -type f -size +1M | wc -l` returns 0 — no multi-megabyte blob entered history
    - `src/content/properties/2734-brown-st.md` exists and contains `2734 Brown Street`
    - Both entries pass schema validation: `npm run build` exits 0
    - `grep -c 'downPayment: 3000' src/content/properties/2734-brown-st.md` returns 1 and `grep -c 'monthlyPayment: 1250' src/content/properties/2734-brown-st.md` returns 1
    - The Brown Street entry sets `beds: 4` and `baths: 1` and does not set `sqft`
    - The Marengo entry sets none of `beds`, `baths`, or `sqft`
    - Neither entry sets a `location` value (D-16)
    - `npm run extract:photos` is registered in package.json scripts
    - Re-running the extraction script is idempotent: running it twice produces byte-identical output files
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test "$(ls public/uploads/properties/614-e-marengo-st/*.jpg | wc -l)" -eq 6 && test "$(ls public/uploads/properties/2734-brown-st/*.jpg | wc -l)" -eq 5 && test "$(find public/uploads -type f -size +1M | wc -l)" -eq 0 && node -e "const sharp=require('sharp');const fs=require('fs');const g=d=>fs.readdirSync(d).map(f=>d+'/'+f);const all=[...g('public/uploads/properties/614-e-marengo-st'),...g('public/uploads/properties/2734-brown-st')];Promise.all(all.map(f=>sharp(f).metadata().then(m=>{if(Math.max(m.width,m.height)>2000)throw new Error('oversized: '+f)}))).then(()=>console.log('PASS'))"</automated>
  </verify>
  <done>Eleven correctly-assigned, correctly-resized photos and two complete, schema-valid property content files are committed.</done>
</task>

<task type="auto">
  <name>Task 2: The Browse Homes grid — every home, correctly ordered, never empty-looking</name>
  <read_first>
    - `.planning/phases/01-foundation/01-UI-SPEC.md` — the Copywriting Contract's empty-state heading and body and its "Call for details" row; the Color table's three badge values; the Typography table's Label and Heading roles and the price-gold rule; the Spacing Scale's 24px card-gap value; the Visual Hierarchy row for the Homes grid
    - `.planning/phases/01-foundation/01-CONTEXT.md` — D-05 (one grid sorted by status, Sold retained as social proof), D-10 ("Call for details" placeholder)
    - `src/layouts/Layout.astro` — the shared layout every page renders through
    - `src/content.config.ts` — the status enum and the optional spec fields
  </read_first>
  <files>src/pages/homes/index.astro, src/components/PropertyCard.astro, src/components/StatusBadge.astro</files>
  <action>
Build `/homes` as one grid, per D-05 — not separate sections and not a filtered view.

**`StatusBadge.astro`** renders the three variants with the values pinned in the UI-SPEC Color
section: Available on accent `#FFD053` with ink text, Pending on `#D9B36C` with ink text, Sold on ink
`#1A1A1A` with cream `#FFFDF7` text so it reads as a stamp. The badge carries the status as text, not
colour alone — colour is never the sole carrier of meaning.

**`PropertyCard.astro`** renders cover photo, address at Label size, the beds/baths/sqft meta row,
the price figures at Heading size in price gold, and the status badge. The cover is the first entry
in `photos`. Give the photo a fixed aspect-ratio container so cards keep a common height regardless
of source photo proportions, and set explicit width and height attributes so nothing shifts as images
load. Any absent bed, bath, or square-footage value renders `Call for details` for that field
specifically — not for the whole row, and never a blank cell, a dash, or a zero. 614 E Marengo St
will show it for all three fields; that is the designed outcome, and it converts a data gap into a
phone prompt. Alt text describes what the photo shows, not the filename and not the address again.

Price figures use the price-gold token, not the accent fill. Every bright brand yellow fails contrast
as text on the cream surfaces at roughly 2:1, which is why the price-gold token exists at 3.3:1 for
these 24px figures.

Show the payments as what they are — the down payment and the monthly payment — with enough labelling
that they cannot be read as the total price of the home. Do not add copy implying these are the
complete cost of ownership.

**`src/pages/homes/index.astro`** reads the properties collection and sorts it. The primary key is
status rank: Available, then Pending, then Sold. Ties within a status group break by `publishDate`
descending, then by `slug` ascending as a final tiebreak so the ordering is total and stable across
rebuilds. This matters immediately rather than theoretically: both migrated homes are Available, so
they tie on the primary key on day one, and without a defined tiebreak their order could differ
between builds. Do not rely on the collection's natural read order, which is filesystem-dependent.

When the collection is empty, render the empty state from the Copywriting Contract: the heading
`No Homes Available Right Now`, and the body `Check back soon — new homes are added regularly. Or call us at (217) 269-0003 to ask what's coming next.`
Do not render an empty grid container or a bare page.

Lay the grid out mobile-first: one column at phone width, wrapping to more columns as space allows,
with 24px gaps. No fixed maximum card count and no horizontal scroll.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0 and `dist/homes/index.html` exists
    - That page contains both `614 E Marengo St` and `2734 Brown Street`
    - It contains both monthly figures rendered as currency, and both down-payment figures
    - `grep -c 'Call for details' dist/homes/index.html` returns at least 3 — the Marengo card's three absent specs
    - The Brown Street card shows its 4 beds and 1 bath as values rather than as call-for-details
    - Sort order is correct and stable: temporarily set the Marengo entry's status to Sold, rebuild, and confirm the Brown Street card's byte offset in `dist/homes/index.html` is now lower than the Marengo card's; revert and confirm the original order returns
    - The tiebreak is deterministic: build twice without changing any content and confirm `dist/homes/index.html` is byte-identical across both builds
    - All three badge variants render correctly: temporarily set one entry to Pending and one to Sold, rebuild, and confirm the page contains the text Available, Pending, and Sold appropriately and that the Sold badge uses the ink fill with cream text; then revert both
    - The empty state renders: temporarily move both property files out of the collection directory, rebuild, and confirm `dist/homes/index.html` contains `No Homes Available Right Now` and the phone number; then restore both files
    - Two homes of equal status render two distinct cards — the built page contains exactly 2 card root elements while both are Available
    - Every `img` in `dist/homes/index.html` has a non-empty `alt` attribute
    - The page renders through the shared layout: it contains the Equal Housing sentence and the header phone number
  </acceptance_criteria>
  <verify>
    <automated>npm run build && cp dist/homes/index.html /tmp/homes-build-1.html && npm run build && cmp /tmp/homes-build-1.html dist/homes/index.html && grep -q '614 E Marengo St' dist/homes/index.html && grep -q '2734 Brown Street' dist/homes/index.html && test "$(grep -o 'Call for details' dist/homes/index.html | wc -l)" -ge 3 && test "$(grep -o '<img' dist/homes/index.html | wc -l)" -eq "$(grep -o '<img[^>]*alt="[^"]\+"' dist/homes/index.html | wc -l)" && echo PASS</automated>
  </verify>
  <done>/homes renders both homes as correctly-ordered, deterministically-sorted cards with badges, prices, and call-for-details fallbacks, and degrades to a designed empty state.</done>
</task>

<task type="auto">
  <name>Task 3: The property page — gallery, lightbox, specs, and the status-aware call to action</name>
  <read_first>
    - `.planning/phases/01-foundation/01-CONTEXT.md` — D-08 (cover photo plus thumbnail strip, full-screen lightbox with arrows and swipe), D-09 (Inquire active on Available and Pending, replaced on Sold), and the `<specifics>` block describing the intended classic real-estate feel
    - `.planning/phases/01-foundation/01-UI-SPEC.md` — the Copywriting Contract rows for both property-page CTAs; the Spacing Scale's 44px touch-target exception; the `## UI Considerations` rows for surface E3; the Visual Hierarchy row for the property page
    - `src/pages/homes/[slug].astro` — the route written in plan 01-02, which this task completes
    - `src/components/BrandMark.astro` — the placeholder mark used for the zero-photo frame
    - `src/components/Nav.astro` — the existing mobile drawer toggle, so the lightbox's keyboard and focus handling follows the same conventions rather than inventing a second pattern
  </read_first>
  <files>src/components/Gallery.astro, src/components/gallery-lightbox.ts, src/pages/homes/[slug].astro</files>
  <action>
Complete the property page into the classic real-estate layout D-08 describes.

**`Gallery.astro`** renders a large cover photo (the first entry in `photos`) with a thumbnail strip
beneath it. Every image gets explicit width and height attributes so layout space is reserved and
nothing shifts while loading, and every image below the fold gets native lazy loading. Alt text
describes the view each photo shows. When `photos` is empty, render a single branded placeholder
frame — cream background with the dual-leaf mark at reduced opacity via `BrandMark.astro` — rather
than an empty container or a broken-image icon. No custom broken-image UI is built; a photo that
fails to resolve falls back to the browser's native alt-text rendering, which is why alt text is
required rather than optional.

**`gallery-lightbox.ts`** is the island, hydrated with `client:visible` so it loads only when the
gallery is scrolled into view. Clicking the cover or any thumbnail opens a full-screen lightbox with
previous and next controls and swipe support on touch. Give the previous and next controls a 44px
minimum hit area by padding them, keeping the visible control on the 8pt scale. Make it keyboard
operable: Escape closes, arrow keys move between photos, focus moves into the lightbox on open and
returns to the triggering thumbnail on close, and focus is trapped inside while open. This and the
mobile nav drawer are the only client JavaScript in the project — keep it small and dependency-free
rather than pulling in a lightbox package for what this is.

**`[slug].astro`** renders, through the shared layout: the address as the Display-size H1 with the
status badge beside it, the gallery, the terms (down payment and monthly payment at Heading size in
price gold), the specs row, the description, and the feature bullets. Absent beds, baths, or square
footage render `Call for details` per field.

The terms block states plainly that the full terms, including what happens if payments are not made,
are set out in the written agreement, and links to How It Works. This is the same framing the
canonical one-pager uses, and it is what keeps a bare price pair from reading as the complete cost of
ownership.

**Status-aware call to action, per D-09.** On an Available or Pending home, render
`Inquire About This Home` linking to `/contact?property=<slug>`. Pending homes keep it deliberately —
it captures backup leads for fall-throughs. On a Sold home, that button is not rendered at all;
`See Available Homes` linking to `/homes` takes its place. The query parameter is passed now and read
by the Zoho prefill script in Phase 3; the `/contact` page shell that receives it is built in plan
01-04, so the link resolves rather than 404s.

A Sold page must not read as purchasable. The badge, the absence of an Inquire button, and the
replacement CTA together carry that; do not soften it.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0; both `dist/homes/614-e-marengo-st/index.html` and `dist/homes/2734-brown-st/index.html` exist
    - The Marengo page contains 6 gallery image references and the Brown Street page contains 5
    - The first referenced photo on each page is that home's `photo-01.jpg` — the cover is the first array entry (PROP-01)
    - Every `img` on both property pages has a non-empty `alt` attribute and explicit `width` and `height` attributes
    - Both pages contain their down payment and monthly payment rendered as currency, the full description, and every feature bullet from the content file
    - `grep -c 'Call for details' dist/homes/614-e-marengo-st/index.html` returns 3; the Brown Street page returns 1 (square footage only)
    - Both pages contain `Inquire About This Home` and an href containing `/contact?property=` with the correct slug
    - The Sold path works: temporarily set the Marengo entry's status to Sold, rebuild, and confirm its page contains `See Available Homes` with an href of `/homes` and contains no occurrence of `Inquire About This Home`; then revert and confirm the Inquire CTA returns
    - The Pending path works: temporarily set the entry's status to Pending, rebuild, and confirm `Inquire About This Home` is still present (D-09 keeps it active on Pending); then revert
    - The zero-photo placeholder works: temporarily empty one entry's `photos` array, rebuild, and confirm the page contains a `BrandMark` placeholder reference and no broken image element; then revert
    - The lightbox island is hydrated lazily: the built page contains a `client:visible` hydration marker for the gallery and no other client-side script beyond it and the nav drawer toggle
    - Keyboard operation verified in `npm run preview`: Tab reaches a thumbnail, Enter opens the lightbox, arrow keys change photos, Escape closes it, and focus returns to the thumbnail that opened it
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -f dist/homes/2734-brown-st/index.html && test "$(grep -o 'photo-0[0-9]*\.jpg' dist/homes/614-e-marengo-st/index.html | sort -u | wc -l)" -eq 6 && test "$(grep -o 'photo-0[0-9]*\.jpg' dist/homes/2734-brown-st/index.html | sort -u | wc -l)" -eq 5 && grep -q 'Inquire About This Home' dist/homes/614-e-marengo-st/index.html && grep -q '/contact?property=' dist/homes/614-e-marengo-st/index.html && test "$(grep -o 'Call for details' dist/homes/614-e-marengo-st/index.html | wc -l)" -eq 3 && test "$(grep -o '<img' dist/homes/2734-brown-st/index.html | wc -l)" -eq "$(grep -o '<img[^>]*alt="[^"]\+"' dist/homes/2734-brown-st/index.html | wc -l)" && echo PASS</automated>
    <human-check>Open `/homes/614-e-marengo-st` in `npm run preview` at phone width. Tap a thumbnail: the lightbox should fill the screen, swipe should move between photos, the arrows should be comfortably tappable, and Escape or the close control should return you to the page with the thumbnail still focused.</human-check>
  </verify>
  <done>Both property pages render complete galleries with a working, keyboard-operable lightbox, full terms and specs with call-for-details fallbacks, and the correct status-dependent call to action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mockup HTML → extraction script → committed image files | Base64 payloads decoded and written to disk, then committed permanently. Developer-authored source, but the write is irreversible once committed. |
| content file → build | Property frontmatter validated by Zod before rendering. Phase 1's only input-validation surface (ASVS V5). |
| property description copy → public HTML | Marketing copy about housing, which carries Fair Housing advertising obligations that ordinary product copy does not. |
| built HTML → visitor browser | Static output plus one client-side island (the lightbox). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Information Disclosure | Oversized or unintended image blobs entering permanent git history | high | mitigate | The extraction script resizes to a 2000px longest edge before writing, and writes only into `public/uploads/properties/<slug>/`. Acceptance criteria assert every committed file's dimensions with sharp metadata and that no file exceeds 1 MB, checked before the commit rather than after. |
| T-01-13 | Tampering | Photos mis-assigned between the two homes by a global regex crossing property boundaries | high | mitigate | The script parses each property object individually, keyed on its `address` value, and asserts the exact per-home counts (6 and 5) before writing anything. A count mismatch fails loudly and writes nothing. Mis-assigned listing photos are a material misrepresentation, not a cosmetic bug. |
| T-01-14 | Tampering | Property copy acquiring Fair-Housing-steering language | high | mitigate | Descriptions are ported from the mockup's existing `desc` text, which describes homes rather than occupants, and the task action forbids adding who-this-suits framing. Carried as a flagged prohibition in `must_haves.prohibitions` for judgment review. |
| T-01-15 | Tampering | Sold or Pending status rendering ambiguously enough to read as available | high | mitigate | D-09's CTA replacement is asserted by acceptance criteria that flip the status, rebuild, and require both the presence of the replacement CTA and the absence of the Inquire CTA. Status is also carried as badge text, never by colour alone. |
| T-01-16 | Tampering | Malformed property frontmatter reaching the built site | medium | mitigate | ASVS V5: the Zod schema from plan 01-02 validates both entries; `npm run build` failing is the gate. |
| T-01-17 | Information Disclosure | Metadata (EXIF, GPS coordinates) embedded in extracted photos | low | mitigate | sharp's re-encode pipeline does not carry EXIF through by default, so the JPEG re-encode strips it. The source images are base64 payloads already stripped by the mockup's own export, so no location data is expected in the first place. |
| T-01-18 | Elevation of Privilege | The lightbox island as a client-side script surface | low | accept | Hand-rolled, dependency-free, operating only on build-time-known image paths from validated content. No user input, no network calls, no dynamic evaluation. |

**Canon referral (not minted as prohibitions):** cross-site scripting via rendered content is canon,
owned by `/gsd-secure-phase` and lint tooling; it is additionally near-vacuous here because Astro
escapes interpolated content by default and every value originates in a Zod-validated repo file
rather than from a visitor.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 01-03 contribution)

**Component names:** `PropertyCard.astro`, `StatusBadge.astro`, `Gallery.astro`

**Client island:** `src/components/gallery-lightbox.ts` (hydrated `client:visible`)

**Routes:** `/homes` (`src/pages/homes/index.astro`); `/homes/614-e-marengo-st` and
`/homes/2734-brown-st` generated from `src/pages/homes/[slug].astro`

**Scripts:** `scripts/extract-mockup-photos.mjs`

**npm scripts:** `extract:photos`

**Content files:** `src/content/properties/614-e-marengo-st.md` (completed),
`src/content/properties/2734-brown-st.md` (created)

**Static assets:** `public/uploads/properties/614-e-marengo-st/photo-01.jpg` … `photo-06.jpg`;
`public/uploads/properties/2734-brown-st/photo-01.jpg` … `photo-05.jpg`
</artifacts_this_phase_produces>

<verification>
- Eleven photos exist, correctly split 6 and 5, none exceeding a 2000px longest edge or 1 MB.
- `/homes` lists both homes, sorted Available before Pending before Sold, byte-identical across two consecutive builds.
- The empty state, all three badge variants, the Sold CTA replacement, the Pending CTA retention, and the zero-photo placeholder are each proven by a temporary content change followed by a revert.
- Every image on the grid and both property pages has non-empty alt text.
- The lightbox is keyboard operable end to end.
</verification>

<success_criteria>
1. Both real Oak Homes properties are browsable from the grid to their own permanent URLs with their real photos and real terms.
2. Photos entered git history already resized — the irreversible step was done correctly the first time.
3. Data gaps render as phone prompts rather than blanks, so no card or page looks broken.
4. A Sold home cannot be mistaken for a purchasable one.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done. Record the extracted photos'
actual dimensions so a later phase can judge whether a full-resolution reshoot is warranted.
</output>
