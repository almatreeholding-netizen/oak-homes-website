---
phase: 01-foundation
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - src/layouts/Layout.astro
  - src/components/Nav.astro
  - src/components/PropertyCard.astro
  - src/components/Gallery.astro
  - src/components/gallery-lightbox.ts
  - src/components/Button.astro
  - src/components/StatusBadge.astro
  - src/styles/global.css
  - src/pages/
  - README.md
  - scripts/verify/checks.mjs
autonomous: true
requirements: [DESIGN-04, DESIGN-05, INFRA-02]

estimate:
  tokens: 60000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # --- DESIGN-04: WCAG 2.1 AA basics ---
    - "Every img element in every built page has an alt attribute — descriptive for meaningful images, or empty and paired with aria-hidden for decorative ones. No image is missing the attribute entirely."
    - "Every page declares a document language and a utf-8 character encoding."
    - "Body text, price figures, badge text, and button text all meet WCAG 2.1 AA contrast against their actual background: 4.5:1 for text below 24px and 3:1 for text at 24px and above."
    - "Accent yellow is used as a fill only and never as text on either cream surface, where it measures roughly 2:1 and would fail AA."
    - "The whole site is operable by keyboard alone: every interactive element is reachable by Tab in a sensible order, has a visible focus indicator, and is activatable by Enter or Space."
    - "The mobile nav drawer and the gallery lightbox both trap focus while open, close on Escape, and return focus to the control that opened them."
    - "Every page has exactly one h1 and a heading structure that descends without skipping levels."
    - "Status is conveyed by badge text and not by colour alone, so a visitor who cannot distinguish the badge colours still knows whether a home is Available, Pending, or Sold."
    - "The site uses a main landmark, a nav landmark, and a footer landmark, and offers a skip-to-content link as the first focusable element."
    # --- DESIGN-04 edges (spec-less probe fallback, resolved explicit) ---
    - "DESIGN-04/empty: no image ships with a missing alt attribute; a decorative image uses an empty alt only when it is also marked aria-hidden, so an empty alt is always a deliberate decision rather than an omission."
    - "DESIGN-04/encoding: every page declares utf-8 and a document language, and the em dashes, apostrophes, and curly quotes in the ported legal copy render as their exact source characters in the built HTML rather than as replacement characters or escapes."
    # --- DESIGN-05: mobile-first responsive ---
    - "Every page reads correctly at 320px, 375px, 768px, and 1280px with no horizontal scrolling and no clipped or overlapping content."
    - "The build emits pre-rendered static HTML for every route with no server adapter configured."
    # --- LEAD-03 edges (spec-less probe fallback, resolved backstop) ---
    - { statement: "LEAD-03/boundary: the phone call-to-action keeps a 44px minimum hit area at the narrowest supported viewport (320px) and remains visible without horizontal scrolling — verify by measuring the rendered tap target in browser devtools at 320px width.", verification: backstop }
    - { statement: "LEAD-03/adjacency: exactly one phone call-to-action is visible at any viewport width — the header CTA and any persistent mobile phone element must not both render at the breakpoint where their media queries meet — verify by sweeping widths across the 768px boundary.", verification: backstop }
    # --- INFRA-02: the repo is the source of truth ---
    - "The complete site source, content, and images are pushed to the private GitHub repository, and remote main matches local HEAD."
    - "A README documents the local full-stack run command, the photo pre-resize convention, and the Phase 2 and Phase 3 extension points."
    - "Every internal link in the built site resolves to a page that exists — no nav item, CTA, or Inquire button 404s."
    # --- ROADMAP success criterion 3, completed: branding on every page ---
    - "Every built page carries all four elements ROADMAP criterion 3 names: the Equal Housing sentence, the phone number (217) 269-0003, the tagline `From Rent to Roots` (as the header brand mark's accessible name, per plan 01-02 Task 4), and the marked integrations slot — so the criterion is verified as a set rather than three of four."
  artifacts:
    - path: "README.md"
      provides: "Local run instructions, the ~2000px photo convention, and the marked extension points later phases build on"
      contains: "npm run build"
  key_links:
    - from: "local main branch"
      to: "github.com/almatreeholding-netizen/oak-homes-website"
      via: "push to the private origin established in plan 01-01"
      pattern: "origin"
  prohibitions:
    - statement: "MUST NOT use alt text that is search-engine keyword filler, a filename, a bare repetition of the property address, or an empty string on a meaningful image. Alt text exists to tell a visitor who cannot see the photo what the photo shows; treating it as an SEO field silently converts an accessibility feature into a marketing one and leaves the visitor it was built for with nothing."
      category: values
      status: unverified
      verification: judgment
      flagged: true
---

<!-- planner-region-allow: Equal Housing Opportunity -->
<!-- planner-region-allow: agreement for deed -->
<!-- planner-discipline-allow: equitable interest -->
<!-- planner-discipline-allow: honest terms -->
<!-- planner-discipline-allow: not a rental -->
<!-- planner-discipline-allow: FROM RENT TO ROOTS -->

> **Allowlist note.** This plan's whole-site sweep runs the phase's region-scoped gates over the
> assembled output, so the same literals appear on both sides of opposite assertions: the Equal
> Housing sentence and `agreement for deed` are required **present** across `dist/` and required
> **absent** under `src/content/` (DESIGN-03). The retired-phrasing literals and the all-caps
> wordmark are negative-only. All of them appear in this plan **exclusively inside
> `<acceptance_criteria>` and in `scripts/verify/checks.mjs`** — never inside an `<action>` body,
> which is the text an executor transcribes from when authoring copy. A literal that lives only on
> the checking side cannot be copied into the site by following the instructions.

<objective>
Make the finished site usable by everyone on every device, prove it end to end, and put it in the
repository as the phase's deliverable.

Purpose: Accessibility and responsiveness were built into each earlier plan's acceptance criteria
rather than deferred wholesale to here — this plan is the cross-cutting sweep that catches what only
becomes visible once all seven pages exist together, and the phase-level proof that Phase 1's
definition of done is met: a clean local build and a push to the private repo, not a live URL.
Deployment is INFRA-03 in Phase 2.

Output: A WCAG 2.1 AA-basics-compliant, mobile-first site, fully pushed, with a README that tells the
next phase where its extension points are.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/SKELETON.md
</context>

<shell_contract>
## Executor shell contract — read before running any `<verify>` block

**The executor session's shell on this machine is Windows PowerShell 5.1**, which is not POSIX:
`&&` is a parse error, `$(...)` is rejected, `for f in $(find ...)` does not parse at all, and
`grep`, `find`, `wc`, and `test` do not exist as commands. This plan's original verify blocks were
the most shell-dependent in the phase — a `for` loop over `find` output and a `grep -rl $'�'`
ANSI-C quoted literal, neither of which has any meaning outside bash.

Every `<automated>` block in this plan is therefore exactly one command:

```
node scripts/verify/checks.mjs <check-id>
```

`scripts/verify/checks.mjs` was created in plan 01-01 Task 2; its full authoring rules are in that
plan's `<shell_contract>`. Node built-ins only, no shelling out to POSIX utilities, `spawnSync` with
an argv array (`npm.cmd` on win32), unknown check id exits non-zero, and a check that cannot run its
assertion fails loudly rather than passing. Searching for the Unicode replacement character is
`text.includes('�')` in Node — an escape the shell never sees.

This plan registers two checks: `a11y-sweep` (Task 1) and `phase-complete` (Task 2).
`phase-complete` is the phase's final gate and re-runs the cross-cutting assertions over the whole
assembled `dist/` tree.
</shell_contract>

<review_response>
## Cross-AI review findings addressed in this plan

| Finding | Severity | Disposition |
|---|---|---|
| All `<automated>` blocks are Bash; the session shell is PowerShell 5.1 — and this plan's are the worst offenders (`for f in $(find ...)`, `grep -rl $'�'`) | HIGH | **Incorporated.** `<shell_contract>` above; both verify blocks are a single `node scripts/verify/checks.mjs <id>` invocation. |
| ROADMAP criterion 3 requires the "From Rent to Roots" tagline on every page, but no plan asserted it appears at all — 01-02 only prohibited re-typesetting it as a wordmark, and this plan's whole-site sweep checked the Equal Housing line and the phone number but not the tagline | LOW | **Incorporated.** Plan 01-02 Task 4 pins the header brand mark's alt text to `Oak Homes — From Rent to Roots`; this plan's Task 2 now asserts that literal on every built page alongside the Equal Housing sentence, the phone number, and the integrations slot, so criterion 3 is verified as a complete set. Asserting it via the accessible name rather than as page copy is what keeps it compatible with the UI-SPEC rule forbidding re-typesetting the lockup. |
| The third retired-phrasing grep tests a paraphrase nobody would type, so it passes regardless of whether the risk is present | MEDIUM | **Incorporated.** This plan's site-wide sweep uses the same corrected literal set as 01-04: `equitable interest`, `honest terms`, and `not a rental` (the substring real drift would contain), with the paraphrase grep removed. |
| UI-SPEC line 160 says card price figures use "accent" while lines 101/104 say price figures use price gold and accent is never text on cream; the plans correctly follow the latter, but a future reader could follow line 160 | LOW | **Incorporated outside the plan set.** `01-UI-SPEC.md`'s `populated / E2` row was corrected to name price gold, with the contradiction noted inline so the correction is not silently re-introduced. No plan behaviour changes — all plans already followed the correct rule. |
</review_response>

<tasks>

<task type="auto">
  <name>Task 1: Site-wide accessibility and responsive sweep</name>
  <read_first>
    - `.planning/phases/01-foundation/01-UI-SPEC.md` — the Color table's contrast rationale (why price gold exists at 3.3:1 and why the accent is fill-only), the Spacing Scale's 44px touch-target exception and the four elements it names, the Typography table's four sizes and two weights, and the `## UI Considerations` rows for every surface
    - `.planning/phases/01-foundation/01-RESEARCH.md` — the DESIGN-04 row in the phase-requirements table and its WCAG checklist references
    - `src/layouts/Layout.astro`, `src/components/Nav.astro`, `src/components/Gallery.astro`, `src/components/gallery-lightbox.ts`, `src/components/Button.astro`, `src/components/StatusBadge.astro`, `src/components/PropertyCard.astro` — every interactive and image-bearing surface built in plans 01-02 through 01-04
    - `src/styles/global.css` — the `@theme` token block, where any contrast correction must land as a token change rather than as a one-off override
  </read_first>
  <files>src/layouts/Layout.astro, src/components/Nav.astro, src/components/Gallery.astro, src/components/gallery-lightbox.ts, src/components/Button.astro, src/components/StatusBadge.astro, src/components/PropertyCard.astro, src/styles/global.css, src/pages/</files>
  <action>
Audit the assembled site against WCAG 2.1 AA basics and the responsive contract, and fix what fails.
Prefer fixing at the token or component level over patching individual pages — a contrast failure on
one badge is a token problem, not a page problem.

**Images.** Confirm every image across every built page carries an alt attribute. Meaningful images
describe what they show; a property photo's alt says what the photo depicts, not the filename and not
a second copy of the address that the adjacent heading already announces. Decorative marks use an
empty alt paired with `aria-hidden` so a screen reader skips them rather than announcing the brand
twice. An image with no alt attribute at all is the failure this checks for — an empty alt is only
correct when it is deliberate and paired.

**Contrast.** Measure actual rendered foreground-on-background pairs, not intentions: body copy and
label text need 4.5:1, and text at 24px and above needs 3:1. Check the three badge variants against
their own fills, the price figures against both cream surfaces, button text against the accent fill,
and the footer text against the ink footer. The accent is a fill colour only — if it has become text
on cream anywhere, that is a failure to fix, because it lands near 2:1. Price figures use the
price-gold token, which the UI-SPEC records at 3.3:1 on both creams and therefore passing for the
24px figures but not for anything smaller.

**Keyboard operation.** Tab through every page start to finish. Every interactive element must be
reachable in an order that matches the visual layout, carry a clearly visible focus indicator, and
activate with Enter (and Space, for anything behaving as a button). The two client-side islands need
particular care: both the mobile nav drawer and the gallery lightbox must trap focus while open,
close on Escape, and return focus to the element that opened them. A drawer or lightbox that leaves
focus behind on the page underneath is the most common failure here.

Add a skip-to-content link as the first focusable element on the page, targeting the main landmark.

**Semantics and landmarks.** Every page needs exactly one h1 and a heading order that descends
without skipping a level. Confirm the nav, main, and footer landmarks are real elements rather than
styled divs. Confirm the document language and character encoding are declared, and that the em
dashes, apostrophes, and curly quotes in the ported legal copy survive the build as their exact
source characters rather than as replacement characters or escapes — that copy is legally refined and
a mangled character in it is a real defect.

Confirm status is never carried by colour alone: each badge states its status as text.

**Responsive.** Check every page at 320px, 375px, 768px, and 1280px. Nothing may scroll horizontally,
clip, or overlap. Give particular attention to the longest real address on a card at 375px, the
seven nav labels at exactly 768px (if they do not fit on one line, raise the hamburger breakpoint
rather than shrinking the type below the four-size scale), and the gallery lightbox controls at
320px. Confirm the 44px minimum hit area holds at the narrowest width for the four elements the
UI-SPEC names: the phone CTA, the nav hamburger, the lightbox arrows, and the Inquire button. Confirm
exactly one phone CTA is visible at any given width — the header CTA and any persistent mobile phone
element must not both appear where their media queries meet.

Fix every failure found. Where a fix would require a fifth font size or a colour outside the token
set, change the layout or spacing instead — the type scale and the token set are the contract.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Across every built HTML file, the count of `<img` occurrences equals the count of img elements carrying an `alt` attribute — zero images are missing the attribute
    - Every img with an empty alt also carries `aria-hidden`: the count of empty-alt images equals the count of images having both an empty alt and `aria-hidden`
    - Every built page contains a document language declaration and a utf-8 charset declaration
    - Encoding survived: the built How It Works page contains the em dash and apostrophe characters exactly as they appear in `docs/reference/Oak-Homes-How-It-Works.html`, verified by re-running plan 01-04's transcription comparison — the same five-step pipeline (strip tags, decode HTML entities, NFC-normalise, collapse whitespace, trim) applied to both sides, so a correct page cannot false-fail on `&amp;` or on a decomposed curly apostrophe — with zero occurrences of the Unicode replacement character anywhere under `dist/`
    - Every built page contains exactly one `<h1` occurrence
    - Every built page contains a `<main`, a `<nav`, and a `<footer>` element, and a skip link whose href targets the main landmark
    - The accent is never text on cream: no built stylesheet rule sets a text colour to the accent value
    - Each of the three badge variants renders its status as text — the built grid contains the word Available; with statuses temporarily flipped, it contains Pending and Sold as text, then revert
    - No page scrolls horizontally at 320px, 375px, 768px, or 1280px — verified in `npm run preview` at each width
    - The nav hamburger, the phone CTA, the lightbox arrows, and the Inquire button each present at least a 44px hit area at 320px, measured in devtools
    - Keyboard round trip verified on `/homes/614-e-marengo-st`: skip link first, Tab order matches visual order, focus indicators visible throughout, the lightbox traps focus and returns it on Escape, and the mobile drawer does the same
    - Exactly one phone CTA is visible at 320px, 767px, 768px, and 1280px
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify/checks.mjs a11y-sweep</automated>
    <human-check>Open `npm run preview` and walk the whole site at 375px and again at 1280px. Confirm nothing scrolls sideways, the phone number is easy to tap on a phone, the nav drawer opens and closes cleanly, and the gallery lightbox opens, moves between photos, and closes with Escape. Then Tab through the property page without touching the mouse and confirm you can always see where focus is.</human-check>
  </verify>
  <done>The assembled site passes WCAG 2.1 AA basics — alt text, contrast, keyboard operability, landmarks, and focus management — and reads correctly from 320px to 1280px.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the whole phase and push it to the private repo</name>
  <precondition>origin resolves to the private repository https://github.com/almatreeholding-netizen/oak-homes-website.git and the repository is Private (plan 01-01 complete).</precondition>
  <read_first>
    - `.planning/ROADMAP.md` — the Phase 1 section's five success criteria, which this task verifies as a set rather than plan by plan. Criterion 3 names four things every page must carry — branding, the phone number, the Equal Housing footer, and the integrations slot — and its branding clause explicitly includes the tagline, which is why the sweep below checks four literals rather than two
    - `.planning/phases/01-foundation/01-02-SUMMARY.md` — the recorded alt text of the header brand mark, which is the exact literal the tagline assertion greps for
    - `.planning/phases/01-foundation/SKELETON.md` — the "Stack Touched in Phase 1" checklist and the skeleton invariants, both of which are confirmed here
    - `package.json` — the existing scripts, so the README documents the real commands rather than assumed ones
  </read_first>
  <files>README.md</files>
  <action>
Run the phase-level proof, write the handoff README, and push.

**Whole-site verification.** From a clean state, run the build and confirm every route emits: the
homepage, `/homes`, both property pages, How It Works, About, Learn index, the seeded Learn post,
Schedule, and Contact. Confirm the sitemap generated.

Then check the cross-cutting invariants that only hold across the assembled site:

- Every built page carries the Equal Housing sentence exactly once.
- Every built page carries the phone number.
- Every built page carries the tagline `From Rent to Roots` as the header brand mark's accessible
  name. This is the fourth element of ROADMAP success criterion 3 and the one no earlier plan
  asserted; check it here alongside the other three rather than assuming the logo implies it.
- Every built page carries the integrations-slot marker exactly once.
- No page under `dist/` contains either retired literal from
  `docs/specs/2026-08-28-oak-homes-website-design.md` section 6, nor the substring real drift on the
  third retired framing would contain.
- No built stylesheet contains the superseded design-spec yellow estimate; the accent and price-gold
  token values are present.
- No legally-sensitive copy exists anywhere under `src/content/`.
- Every internal link resolves. Collect every site-relative href from every built page, strip query
  strings and fragments, and confirm each maps to a file that exists under `dist/`. This is the check
  that catches a nav item or an Inquire button pointing at a page that was never built.
- The build is reproducible: two consecutive builds produce identical output.

Fix anything that fails before proceeding — this is the gate, not a report.

**README.** Write it for the next person, which is likely this project three phases from now. Cover:
what the project is and the stack it runs on; the local commands (install, dev, build, preview) taken
from the real `package.json`; the content model, naming the three collections and where their files
live; the photo convention, stating that property photos are pre-resized to roughly 2000px before
being committed and why (git retains every committed blob permanently); how to add a home by editing
only its markdown file and photos, with no template edit required; and the marked extension points
later phases fill — the site-wide integrations slot in the layout, the Zoho embed slot on the contact
page, and the `location`, `videoUrl`, and `ogImage` schema fields that are defined but unused until
Phase 3. Note explicitly that deployment is Phase 2 and that Phase 1's done state is a clean local
build plus a push.

**Push.** Commit everything and push to the private origin. Confirm remote main matches local HEAD,
and confirm the working tree is clean afterwards so nothing was left unstaged.

Do not configure Netlify, do not add a deployment adapter, and do not add a CI workflow — INFRA-03 is
Phase 2, and adding it here would put an untested deploy path in front of the phase that is supposed
to build and verify it.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0 and all ten expected routes emit an index.html: `/`, `/homes`, `/homes/614-e-marengo-st`, `/homes/2734-brown-st`, `/how-it-works`, `/about`, `/learn`, `/learn/what-is-a-land-contract`, `/schedule`, `/contact`
    - A sitemap index file exists under `dist/`
    - **ROADMAP criterion 3 as a complete set.** The number of `.html` files under `dist/` equals, separately, the number of those files containing each of: the exact Equal Housing sentence; the display phone `(217) 269-0003`; the tagline literal `Oak Homes — From Rent to Roots` (the header brand mark's alt text, set in plan 01-02 Task 4); and the integrations-slot marker comment. Four independent counts, all equal to the page count — the tagline assertion is new here and is what completes the criterion, which previously checked only the first two
    - The tagline is not re-typeset while being present: no `.astro` file under `src/` contains the all-caps wordmark literal `FROM RENT TO ROOTS`, even though every built page carries the title-case tagline in the mark's accessible name
    - Zero files under `dist/` contain `equitable interest`, `honest terms`, or `not a rental` (all case-insensitive). These are the corrected literals from plan 01-04 — the previous third grep tested the design spec's *paraphrase* of the retired framing rather than any phrasing an author would produce, and has been replaced
    - Zero files under `dist/` contain `F6C84C` in any case, while the built CSS contains `#FFD053` and `#A87E24`
    - Zero files under `src/content/` contain `Equal Housing Opportunity` or `agreement for deed`
    - Every site-relative internal link resolves to an existing file under `dist/` — zero unresolved links, checked across all built pages after stripping query strings and fragments
    - Two consecutive builds produce identical output for every HTML file
    - `README.md` exists and contains `npm run build`, names all three content collections, states the roughly 2000px photo convention, and names the integrations slot
    - `git status --short` is empty after the final commit
    - `git ls-remote origin refs/heads/main | cut -f1` equals `git rev-parse HEAD`
    - The repository still reports Private — re-confirmed before the final push
    - All five ROADMAP Phase 1 success criteria are individually confirmed and recorded in the summary
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify/checks.mjs phase-complete</automated>
    <human-check>Confirm on github.com that the repository still shows the Private badge and that its file tree now contains the full site — src, public with both homes' photos, docs/reference, and README.md.</human-check>
  </verify>
  <done>The whole phase is verified as a set, the README hands off cleanly to Phase 2, and the complete site lives in the private repository with remote main matching local HEAD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository → GitHub remote | The final push. The visibility invariant established in plan 01-01 is re-confirmed here because this push carries the entire site, not just documentation. |
| built HTML → visitor browser, including assistive technology | Accessibility is a correctness property of this boundary: a page that renders visually but is unusable by keyboard or screen reader has failed at it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-26 | Information Disclosure | The full site pushed to a repository that is no longer Private | high | mitigate | Task 2 carries a `<precondition>` asserting the private origin and re-confirms the Private badge before the final push. This push carries far more than plan 01-01's did, so the invariant is re-checked rather than assumed to have held. |
| T-01-27 | Tampering | Legally-refined copy silently mangled by character-encoding loss during the build | high | mitigate | Every page is asserted to declare utf-8, the ported land-contract paragraph is compared character-for-character against the canonical source after normalisation, and the whole `dist/` tree is asserted free of the Unicode replacement character. Encoding damage to legally-refined wording is a compliance defect, not a cosmetic one. |
| T-01-28 | Denial of Service | Visitors excluded by inaccessible markup — keyboard-only, screen-reader, or low-vision users unable to complete the phone-call conversion path | high | mitigate | The full DESIGN-04 sweep in Task 1: alt attributes on every image, measured contrast at both AA thresholds, keyboard operability with visible focus and focus trapping on both islands, landmarks, a skip link, and status conveyed as text rather than colour alone. Framed as denial of service because the effect is a class of visitor unable to use the site at all. |
| T-01-29 | Tampering | A retired phrasing or the superseded brand colour surviving anywhere in the assembled output | medium | mitigate | Site-wide negative greps over the whole `dist/` tree in Task 2, run once all seven pages exist together — the only point at which a cross-page leak is detectable. |
| T-01-30 | Spoofing | A broken internal link sending a visitor to a 404 mid-conversion | medium | mitigate | Every site-relative href across every built page is resolved against the emitted file tree. This specifically catches the nav-and-Inquire links that were authored in plan 01-02 before their destination pages existed. |
| T-01-31 | Tampering | An untested deployment path added ahead of the phase that owns it | low | mitigate | The task explicitly forbids configuring Netlify, adding an adapter, or adding CI. INFRA-03 is Phase 2, which builds and verifies the deploy pipeline properly. |

**Canon referral (not minted as prohibitions):** transport security, security headers, and CSP belong
to the hosting configuration introduced in Phase 2 and are canon concerns owned by
`/gsd-secure-phase`; there is no hosting layer in Phase 1 to attach them to.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 01-05 contribution)

**Files created:** `README.md` — local run commands, content model, photo convention, extension points

**Files modified:** accessibility and responsive corrections across `src/layouts/Layout.astro`,
`src/components/*.astro`, `src/components/gallery-lightbox.ts`, `src/styles/global.css`, and
`src/pages/`

**New shared UI element:** skip-to-content link in `Layout.astro`

**Check ids added to `scripts/verify/checks.mjs`:** `a11y-sweep`, `phase-complete`

**Repository state:** complete site source, content, and images on private `origin/main`

**No new routes, schemas, components, or npm scripts are introduced by this plan.**
</artifacts_this_phase_produces>

<verification>
- `node scripts/verify/checks.mjs a11y-sweep` and `... phase-complete` each exit 0.
- All ten routes build; the sitemap generates; two consecutive builds are identical.
- Every page carries all four ROADMAP-criterion-3 elements — the Equal Housing sentence, the phone number, the `From Rent to Roots` tagline, and the integrations slot; none carries a retired phrasing or the superseded yellow.
- Every image has an alt attribute; every empty alt is paired with aria-hidden.
- Every page declares utf-8 and a document language, has exactly one h1, and has nav, main, and footer landmarks plus a skip link.
- Every internal link resolves to an emitted page.
- The working tree is clean and remote main matches local HEAD on a repository still marked Private.
</verification>

<success_criteria>
1. The site meets WCAG 2.1 AA basics — a keyboard-only or screen-reader visitor can browse homes and reach the phone number.
2. Every page reads correctly from 320px to 1280px with no horizontal scrolling.
3. All five ROADMAP Phase 1 success criteria are confirmed together, not just plan by plan.
4. The private repo holds the complete site, and the README tells Phase 2 where to plug in.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done. Record each of the five ROADMAP
Phase 1 success criteria with its evidence, tick the SKELETON.md "Stack Touched in Phase 1"
checkboxes, and list the two backstop items (the 44px phone target at 320px, and the single-visible-
phone-CTA sweep) with the result of their visual verification.
</output>
