---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/pages/index.astro
  - src/pages/how-it-works.astro
  - src/pages/about.astro
  - src/pages/schedule.astro
  - src/pages/contact.astro
  - src/pages/learn/index.astro
  - src/pages/learn/[slug].astro
  - src/content/blog/what-is-a-land-contract.md
  - src/components/StepList.astro
autonomous: true
requirements: [BROWSE-03, EDU-01, EDU-02, EDU-03, EDU-04, EDU-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- BROWSE-03: the homepage ---
    - "The homepage shows the brand intro read from src/content/settings.json, a 3-step overview of how buying from Oak Homes works, and featured available homes."
    - "Featured homes are selected by the `featured` boolean on the Property schema, filtered to Available status (D-06)."
    - "When no Available home is marked featured, the homepage section falls back to the newest Available homes rather than rendering empty (D-07) — proven by temporarily clearing the featured flag and rebuilding."
    # --- EDU-01 / EDU-02: How It Works ---
    - "The How It Works page carries the land-contract explainer transcribed character-for-character from docs/reference/Oak-Homes-How-It-Works.html, including the sentence stating that the full terms — including what happens if payments aren't made — are set out in the written agreement."
    - "None of the three retired phrasings enumerated in docs/specs/2026-08-28-oak-homes-website-design.md section 6 appears anywhere in the built site."
    - "The How It Works page carries the four-step process and the 'What you'll typically need' panel from the canonical one-pager."
    - "A FAQ section is folded into the How It Works page answering the trust questions the design spec names, authored fresh because neither source mockup contains any FAQ copy to port."
    - "The land-contract explainer lives in a .astro page file, not in a content collection, so Phase 2's CMS cannot expose it as an editable field (DESIGN-03)."
    # --- EDU-03 / EDU-05 / contact shell ---
    - "The About page introduces the company, with copy adapted from the mockup's About section."
    - "The Schedule a Showing page presents the phone call to action reading 'Call (217) 269-0003' and linking to tel:+12172690003."
    - "A /contact page exists so that all 7 nav destinations and every property Inquire link resolve rather than 404; it carries the phone CTA and a clearly marked slot where Phase 3 lands the Zoho web-to-lead embed."
    # --- EDU-04: Learn ---
    - "The Learn index lists published posts, and each post has its own page at /learn/<slug>."
    - "At least one post exists at phase completion: the seeded 'What is a land contract?' explainer (D-15)."
    - "The seeded post is marked as awaiting owner review via the blog schema's ownerReviewed flag, and the page surfaces that state rather than presenting unreviewed placeholder copy as settled guidance."
    # --- UI-SPEC ## UI Considerations, surfaces E7 Learn index / E8 Learn post / E9 static pages / E10 contact-schedule ---
    - "UI-SPEC loading/E7,E8,E9,E10: all four surfaces are static pre-rendered HTML containing no spinners, no skeletons, and no runtime fetch code."
    - "UI-SPEC error/E7,E8,E9,E10: no runtime fetch means no visitor-facing error state on any of these surfaces; a malformed content file fails the build instead."
    - "UI-SPEC empty/E7: the seeded post guarantees at least one entry at phase completion, so no Learn-index empty state needs designing."
    - "UI-SPEC empty/E8: title, date, and body are required blog schema fields; an absent optional coverImage renders a header with no image slot rather than an empty box or a broken-image icon."
    - "UI-SPEC empty/E9,E10: static page content is hand-authored in code rather than data-driven, so these pages cannot render contentless."
    - "UI-SPEC populated/E7: the post list reads correctly at a single post and scales to many without redesign."
    - "UI-SPEC populated/E8: a post renders title, date, optional cover image, and prose body inside a max-width container."
    - "UI-SPEC partial/E7,E8: coverImage is the only optional field; when absent the layout renders without the image slot."
    - "UI-SPEC partial/E9,E10: no dynamic data exists on these pages, so no partial state is reachable."
    - "UI-SPEC overflow/E8,E9: prose renders in a max-width container with normal word wrap and no clamping or ellipsis, especially on legally-sensitive copy."
    - "UI-SPEC zero-one-many/E7: at least one post is guaranteed and the list layout scales from one to many without redesign."
    - "UI-SPEC long-text/E8,E9: prose wraps normally in the max-width container with no truncation, clamping, or ellipsis on descriptive or legally-sensitive copy."
    - "UI-SPEC long-text/E10: the phone-CTA copy is fixed at build time per the Copywriting Contract, so no truncation strategy is needed."
    - { statement: "UI-SPEC overflow/E7: the Learn index grows vertically with no pagination in Phase 1 — acceptable at expected post volume; revisit pagination if the index exceeds roughly 20 posts.", verification: backstop }
  artifacts:
    - path: "src/pages/how-it-works.astro"
      provides: "The land-contract explainer (verbatim), the four-step process, the what-you'll-need panel, and the freshly authored FAQ"
      contains: "agreement for deed"
      min_lines: 60
    - path: "src/pages/index.astro"
      provides: "Brand intro, 3-step overview, featured available homes with fallback"
      contains: "featured"
    - path: "src/pages/about.astro"
      provides: "Company introduction"
    - path: "src/pages/schedule.astro"
      provides: "Phone-first call to action page"
      contains: "tel:+12172690003"
    - path: "src/pages/contact.astro"
      provides: "Contact page shell with the marked Phase 3 Zoho embed slot, receiving the property query parameter from Inquire buttons"
    - path: "src/pages/learn/index.astro"
      provides: "Blog index"
    - path: "src/pages/learn/[slug].astro"
      provides: "Per-post pages generated from the blog collection"
      contains: "getStaticPaths"
    - path: "src/content/blog/what-is-a-land-contract.md"
      provides: "The seeded first Learn post, flagged as awaiting owner review"
      contains: "ownerReviewed"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/PropertyCard.astro"
      via: "featured-with-fallback query rendering the same card component the grid uses"
      pattern: "PropertyCard"
    - from: "src/pages/learn/[slug].astro"
      to: "src/content/blog/"
      via: "getStaticPaths over getCollection('blog')"
      pattern: "getCollection\\(['\"]blog['\"]\\)"
    - from: "src/pages/homes/[slug].astro"
      to: "src/pages/contact.astro"
      via: "Inquire button href carrying the property slug as a query parameter, read by Phase 3's prefill script"
      pattern: "/contact\\?property="
  prohibitions:
    - statement: "MUST NOT state or imply a guarantee of ownership, approval, financing, or credit anywhere in authored copy — no promise that a visitor will qualify, will own the home, or will have their credit repaired. Every claim about terms or outcomes defers to the written agreement. The footer already disclaims that this is not a commitment to lend; body copy must not contradict it."
      category: transparency
      status: unverified
      verification: judgment
      flagged: true
    - statement: "MUST NOT present the seeded Learn post, or the freshly authored FAQ answers, as owner-authored, attorney-reviewed, or legally authoritative guidance. Both are drafted content awaiting the Phase 4 review gate, and copy that reads as settled legal advice before that review is exactly what the review exists to prevent."
      category: transparency
      status: unverified
      verification: judgment
      flagged: true

flagged_assumptions:
  - id: "EDGE-BROWSE-03"
    source: "edge-coverage.json (spec-less probe fallback)"
    status: "unresolved"
    note: "Probe returned `unclassified — review manually`. Planner assumption: the homepage's only real edge is the zero-featured-homes case, which D-07's fallback resolves and which is asserted explicitly by a temporary-flag-clear rebuild. Not silently dropped."
  - id: "EDGE-EDU-01"
    source: "edge-coverage.json (spec-less probe fallback)"
    status: "unresolved"
    note: "Probe returned `unclassified — review manually`. Planner assumption: verbatim copy porting has a transcription-fidelity dimension rather than a data-shape one; it is covered by exact-match greps and by the DESIGN-03 location invariant rather than by an edge predicate. Not silently dropped."
  - id: "EDGE-EDU-02"
    source: "edge-coverage.json (spec-less probe fallback)"
    status: "unresolved"
    note: "Probe returned `unclassified — review manually`. Planner assumption: the FAQ is hand-authored static copy with no inputs, so it has no boundary, ordering, or precision dimension. Its real risk is content correctness, carried as a flagged prohibition. Not silently dropped."
  - id: "EDGE-EDU-03"
    source: "edge-coverage.json (spec-less probe fallback)"
    status: "unresolved"
    note: "Probe returned `unclassified — review manually`. Planner assumption: the About page is hand-authored static copy and cannot render contentless (UI-SPEC empty/E9). Not silently dropped."
  - id: "EDGE-EDU-04"
    source: "edge-coverage.json (spec-less probe fallback)"
    status: "unresolved"
    note: "Probe returned `unclassified — review manually`. Planner assumption: the Learn section's edges are post-count (zero, one, many) and the optional coverImage, all carried explicitly by the UI-SPEC E7/E8 rows plus the overflow/E7 pagination backstop. Not silently dropped."
  - id: "EDGE-EDU-05"
    source: "edge-coverage.json (spec-less probe fallback)"
    status: "unresolved"
    note: "Probe returned `unclassified — review manually`. Planner assumption: the Schedule page is a single fixed phone CTA whose formatting contract is already pinned by the LEAD-03/precision edge in plan 01-02. Not silently dropped."
---

<!-- planner-discipline-allow: agreement for deed -->
<!-- planner-region-allow: agreement for deed -->

> **Allowlist note.** `agreement for deed` is asserted **present** in `dist/how-it-works/index.html`
> and **absent** under `src/content/` — the DESIGN-03 location invariant, which is a region-scoped
> gate rather than a contradiction. The literal must appear verbatim in the task action because it is
> part of the legally-refined wording being transcribed. The three *retired* phrasings are
> deliberately NOT quoted anywhere in this plan; they are referenced by pointer to design spec
> section 6 so they cannot be reintroduced by being copied out of the plan itself.

<objective>
Make every page in the navigation real, so a visitor can move through the whole site — and put the
legally-refined land-contract wording on the page it belongs on, transcribed rather than rewritten.

Purpose: This plan finishes the visitor-facing surface and carries the phase's highest-consequence
content work. The How It Works copy is legally refined wording the owner had cleaned up specifically
to remove risky phrasing; it is ported character-for-character, not paraphrased. The FAQ is the
opposite case — research verified that neither source mockup contains any FAQ text at all, so EDU-02
is fresh authoring rather than migration, and it must be grounded in the locked wording rather than
invented independently.

Output: Home, How It Works (with FAQ), About, Learn (index and post), Schedule a Showing, and a
Contact shell — every nav destination and every Inquire link resolving.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/SKELETON.md
@docs/specs/2026-08-28-oak-homes-website-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The homepage — brand intro, 3-step overview, and featured homes that never look empty</name>
  <read_first>
    - `docs/reference/Oak-Homes-Website-SHARE.html` — the home section (search for the `page-home` identifier) for the brand intro headline and the hero copy. Read only that region; the file is 2.6 MB
    - `.planning/phases/01-foundation/01-CONTEXT.md` — D-06 (featured boolean) and D-07 (fallback to newest Available)
    - `.planning/phases/01-foundation/01-RESEARCH.md` — Pattern 2, the featured-with-fallback query
    - `.planning/phases/01-foundation/01-UI-SPEC.md` — the Visual Hierarchy row for the Homepage (Display headline paired with the Browse Homes accent CTA as one anchor block) and the Copywriting Contract's `Browse Homes` CTA
    - `src/components/PropertyCard.astro` — reused as-is for the featured section; do not fork a second card
    - `src/content/settings.json` — the `homepageIntro` value this page renders
  </read_first>
  <files>src/pages/index.astro, src/components/StepList.astro</files>
  <action>
Build the homepage as the site's anchor page, replacing the minimal placeholder version from plan
01-02.

**Hero.** The Display-size headline paired with the accent `Browse Homes` call to action, read as one
anchor block per the UI-SPEC's Visual Hierarchy table. Exactly one accent-coloured element competes
for primary attention in this viewport. The intro text comes from `homepageIntro` in
`src/content/settings.json`, not from a hardcoded string, because the assistant edits it from Phase 2
on.

**3-step overview.** Create `StepList.astro` as a reusable numbered-step component and use it here for
a condensed three-step version: find a home, agree on terms, move in. The canonical one-pager carries
a fuller four-step process and the How It Works page renders all four; the homepage deliberately
shows three per BROWSE-03, and links through to How It Works for the complete version. Keep the step
copy consistent with the one-pager's language rather than inventing a different vocabulary for the
same process.

**Featured homes.** Read the properties collection, filter to Available, then filter to `featured`
true (D-06). If that set is empty, fall back to the newest Available homes sorted by `publishDate`
descending, capped at three (D-07) — the section must never render empty by accident. Render with the
existing `PropertyCard.astro`. Today 614 E Marengo St carries `featured: true` and 2734 Brown Street
does not, so the primary path shows one card; the fallback path is exercised by the acceptance
criteria rather than left unproven.

Below the featured section, link to `/homes` for the full grid.

Say nothing here that promises approval, qualification, or guaranteed ownership. The footer already
states this is not a commitment to lend, and homepage copy must not pull against it.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0 and `dist/index.html` exists
    - The page contains the `homepageIntro` text from `src/content/settings.json` — changing that value and rebuilding changes the rendered page, proving it is not hardcoded
    - The page contains a `Browse Homes` call to action with an href of `/homes`
    - Exactly three steps render in the overview section
    - The featured section contains `614 E Marengo St` and does not contain `2734 Brown Street`, reflecting the current featured flags
    - The D-07 fallback works: temporarily set the Marengo entry's `featured` to false, rebuild, and confirm the homepage still renders at least one property card and does not render an empty section; then revert and confirm the original single featured card returns
    - The fallback excludes unavailable homes: with `featured` false on both entries and one entry temporarily set to Sold, the fallback section renders only the Available home; then revert both changes
    - The page renders through the shared layout — it contains the Equal Housing sentence and the header phone number
    - Every `img` on the page has a non-empty `alt` attribute
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -f dist/index.html && grep -q 'href="/homes"' dist/index.html && grep -q '614 E Marengo St' dist/index.html && grep -q 'Equal Housing Opportunity' dist/index.html && test "$(grep -o '<img' dist/index.html | wc -l)" -eq "$(grep -o '<img[^>]*alt="[^"]\+"' dist/index.html | wc -l)" && echo PASS</automated>
  </verify>
  <done>The homepage renders the settings-driven intro, a three-step overview, and featured available homes with a proven non-empty fallback.</done>
</task>

<task type="auto">
  <name>Task 2: How It Works with the FAQ folded in, plus About, Schedule, and the Contact shell</name>
  <read_first>
    - `docs/reference/Oak-Homes-How-It-Works.html` — **read this file in full.** It is 22 KB and it is the canonical, port-exactly source for every piece of legally-sensitive copy on the How It Works page (D-04). Transcribe from this file, not from memory and not from the larger mockup
    - `docs/specs/2026-08-28-oak-homes-website-design.md` — section 6, which enumerates the three retired phrasings that must not reappear, and section 5, which names the eight pages; and section 7, which describes the lead flow the Contact shell prepares for
    - `.planning/phases/01-foundation/01-RESEARCH.md` — Pitfall 4, which records that neither mockup contains any FAQ copy (verified by grep this session), so EDU-02 is authoring rather than porting, and Open Question 1's recommendation on how to ground the FAQ
    - `docs/reference/Oak-Homes-Website-SHARE.html` — the About section (search for the `page-about` identifier) for the company introduction copy to adapt. Read only that region
    - `.planning/phases/01-foundation/01-UI-SPEC.md` — the Copywriting Contract's phone CTA row, and the Visual Hierarchy rows for the static pages and the Contact / Schedule page
    - `src/layouts/Layout.astro` — the shared layout, which already carries the Equal Housing footer so these pages must not restate it
  </read_first>
  <files>src/pages/how-it-works.astro, src/pages/about.astro, src/pages/schedule.astro, src/pages/contact.astro</files>
  <action>
**How It Works — transcription, not rewriting.** This page carries the copy the owner had refined
specifically to remove risky phrasing. Port it character-for-character from
`docs/reference/Oak-Homes-How-It-Works.html`. Do not paraphrase, do not modernize, do not tighten,
do not "improve the flow", and do not regenerate it from an understanding of what it says. Copy the
following from the source file exactly as written there:

- the opening framing that Oak Homes offers owner financing structured as a land contract, also
  called an agreement for deed;
- the four-step process section with all four step headings and their body copy;
- the "What a land contract means" paragraph in full, including its closing sentence stating that the
  full terms, including what happens if payments aren't made, are set out in the written agreement;
- the "What you'll typically need" panel with its three items;
- the closing call-to-action section with the phone number;
- the page-specific line noting that the page is a general overview and not legal advice.

Three phrasings were deliberately retired from this copy. They are enumerated in
`docs/specs/2026-08-28-oak-homes-website-design.md` section 6 — read them there and ensure none
reappears anywhere on this page or elsewhere on the site. They are not repeated in this plan
precisely so they cannot be reintroduced by being copied out of it.

All of this copy lives in this `.astro` file. It must never become a content-collection entry, because
a field that does not exist in a collection is a field Phase 2's CMS cannot expose for editing —
that structural impossibility is the whole mechanism behind DESIGN-03.

**The FAQ, folded into this page (EDU-02).** Research verified by grep that neither source mockup
contains any FAQ text, so there is nothing to port and this is fresh authoring. Write it, grounded in
the wording immediately above rather than composed independently — the answers must not drift from
the transcribed copy's framing. Cover at minimum the three trust questions the design spec names:
whether this is legitimate, whether the buyer gets the deed, and what happens if a payment is missed.
Answer the deed question and the missed-payment question by deferring to the written agreement, using
the same "written agreement" framing the canonical copy uses — that is both the accurate answer and
the legally careful one. Keep answers short.

Do not write an answer that promises approval, guarantees ownership, or implies a credit outcome. Do
not present these answers as legal advice; they await the same Phase 4 review as the seeded Learn
post.

**About (EDU-03).** Adapt the mockup's About section into a company introduction. Ordinary marketing
copy — no legal claims.

**Schedule a Showing (EDU-05).** A phone-first page whose primary element is the call to action
reading `Call (217) 269-0003` linking to `tel:+12172690003`, both values read from
`src/content/settings.json` rather than retyped. Give it a 44px minimum hit area. Explain briefly
what happens when someone calls.

**Contact shell.** This page exists in Phase 1 for two structural reasons: it is the seventh nav
destination, and every property Inquire button links to it with a property query parameter. Without
it, the nav and every Inquire button would 404. Build the page with the phone call to action and a
clearly marked slot — an HTML comment naming it as the Phase 3 Zoho web-to-lead embed location —
where the form lands in Phase 3. Do not embed a Zoho form, do not build a substitute contact form,
and do not write the query-parameter prefill script; those are LEAD-01 and LEAD-02 in Phase 3. The
page should read as complete to a visitor today, with the phone as the working contact route.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0 and all four pages exist: `dist/how-it-works/index.html`, `dist/about/index.html`, `dist/schedule/index.html`, `dist/contact/index.html`
    - The How It Works page contains the land-contract paragraph transcribed exactly, verified by an exact-string match on its closing sentence about the full terms including what happens if payments aren't made
    - It contains the phrase `agreement for deed`
    - It contains all four step headings from the source file and all three items from the what-you'll-typically-need panel
    - Transcription fidelity is mechanical, not eyeballed: extract the visible text of the "What a land contract means" paragraph from `docs/reference/Oak-Homes-How-It-Works.html` and from the built page, normalise whitespace, and confirm the two strings are identical
    - None of the three retired phrasings from design spec section 6 appears anywhere under `dist/`: `grep -ril 'equitable interest' dist/ | wc -l` returns 0, `grep -ril 'honest terms' dist/ | wc -l` returns 0, and `grep -ril 'purchase not a rental' dist/ | wc -l` returns 0
    - The legal copy is not CMS-reachable: `grep -rl 'agreement for deed' src/content/ | wc -l` returns 0
    - The FAQ renders on the How It Works page and answers at least three questions, including the deed question and the missed-payment question, with both answers referring to the written agreement
    - The Schedule page contains `Call (217) 269-0003` and an href of `tel:+12172690003`
    - The Contact page exists and contains a marked Phase 3 embed slot; `grep -c 'Zoho' src/pages/contact.astro` returns at least 1 (the slot marker naming what lands there)
    - The Contact page contains no form element and no external script tag — the embed is Phase 3
    - Every property Inquire link resolves: for each `/contact?property=<slug>` href in the built property pages, `dist/contact/index.html` exists
    - All four pages render through the shared layout, each containing the Equal Housing sentence exactly once
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -f dist/how-it-works/index.html && test -f dist/about/index.html && test -f dist/schedule/index.html && test -f dist/contact/index.html && grep -q 'agreement for deed' dist/how-it-works/index.html && test "$(grep -rl 'agreement for deed' src/content/ | wc -l)" -eq 0 && test "$(grep -ril 'equitable interest' dist/ | wc -l)" -eq 0 && test "$(grep -ril 'honest terms' dist/ | wc -l)" -eq 0 && test "$(grep -ril 'purchase not a rental' dist/ | wc -l)" -eq 0 && grep -q 'tel:+12172690003' dist/schedule/index.html && test "$(grep -c 'Equal Housing Opportunity' dist/how-it-works/index.html)" -eq 1 && echo PASS</automated>
  </verify>
  <done>How It Works carries the legally-refined copy verbatim plus a freshly authored FAQ, About and Schedule are complete, and the Contact shell resolves every nav and Inquire link with its Phase 3 slot marked.</done>
</task>

<task type="auto">
  <name>Task 3: The Learn section with its seeded first post</name>
  <read_first>
    - `.planning/phases/01-foundation/01-CONTEXT.md` — D-15 (Claude drafts the land-contract-basics post as seeded content; owner review is a Phase 4 gate, not a Phase 1 gate)
    - `src/content.config.ts` — the blog schema written in plan 01-02, including the `ownerReviewed` flag and the slug regex
    - `docs/reference/Oak-Homes-How-It-Works.html` — the grounding source for the post's substance; the post explains the same thing the one-pager does, for a reader arriving from search rather than from a listing
    - `.planning/phases/01-foundation/01-UI-SPEC.md` — the Visual Hierarchy rows for the Learn index and the Learn post, and the `## UI Considerations` rows for surfaces E7 and E8
    - `src/pages/homes/[slug].astro` — the existing `getStaticPaths` pattern to follow rather than re-derive
  </read_first>
  <files>src/pages/learn/index.astro, src/pages/learn/[slug].astro, src/content/blog/what-is-a-land-contract.md</files>
  <action>
Build the Learn section and seed it, so EDU-04's index-plus-post requirement is met by real content
rather than by an empty shell.

**`src/pages/learn/index.astro`** lists posts from the blog collection, newest first, each showing
title at Heading size, date, and cover image when present. A post without a `coverImage` renders its
card with no image slot at all — never an empty box and never a broken-image icon. The list must read
correctly at a single post, which is exactly what it will contain today, and scale to many without a
redesign. No pagination in Phase 1.

**`src/pages/learn/[slug].astro`** generates a page per post with `getStaticPaths` over the blog
collection, following the same pattern the property route already uses. Render title, date, optional
cover image, and the prose body in a max-width container. Prose wraps normally — no clamping, no
ellipsis, no line-count truncation.

**`src/content/blog/what-is-a-land-contract.md`** is the seeded post (D-15), titled around what a
land contract is. Write it as a plain-language explainer for someone who found the site through
search and does not yet know what owner financing means. Ground it in the canonical one-pager's
framing — same vocabulary, same deferral to the written agreement — so a reader moving between the
post and the How It Works page does not encounter two different accounts of the same arrangement.

Set `ownerReviewed` to false. The post is drafted content awaiting the owner's review before launch,
which is a Phase 4 checklist item rather than a Phase 1 blocker. Surface that state on the rendered
page so the copy is not presented as settled, owner-authored, or attorney-reviewed guidance — a short
note that the article is general information and not legal advice, consistent with the line the
canonical one-pager carries, is sufficient and appropriate.

Do not write anything promising approval, qualification, guaranteed ownership, or a credit outcome.
Where the post touches what happens if payments are missed, defer to the written agreement in the
same words the canonical copy uses rather than characterising the consequences independently.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0; `dist/learn/index.html` and `dist/learn/what-is-a-land-contract/index.html` both exist
    - The index lists the seeded post with its title and date
    - The count of generated `dist/learn/*/index.html` pages equals the number of files in `src/content/blog/`
    - The post page renders the full body prose with no truncation or ellipsis
    - The post's frontmatter sets `ownerReviewed: false`, and the rendered page carries a visible note that the article is general information and not legal advice
    - The no-cover-image path is the current reality and renders cleanly: the seeded post has no `coverImage`, and neither the index card nor the post header contains an empty image container or a broken-image element
    - The with-cover-image path works: temporarily add a `coverImage` value referencing an existing brand asset, rebuild, confirm the image renders on both the index card and the post header with non-empty alt text, then revert
    - Slug validation holds: temporarily set the post's slug to a value containing an uppercase letter, confirm `npm run build` exits non-zero, then revert
    - Both pages render through the shared layout, each containing the Equal Housing sentence
    - `grep -ril 'equitable interest' dist/learn/ | wc -l` returns 0, and the same for the other two retired phrasings from design spec section 6
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -f dist/learn/index.html && test -f dist/learn/what-is-a-land-contract/index.html && test "$(find dist/learn -mindepth 2 -name index.html | wc -l)" -eq "$(ls src/content/blog/ | wc -l)" && grep -q 'ownerReviewed: false' src/content/blog/what-is-a-land-contract.md && grep -q 'Equal Housing Opportunity' dist/learn/what-is-a-land-contract/index.html && test "$(grep -ril 'equitable interest' dist/learn/ | wc -l)" -eq 0 && echo PASS</automated>
  </verify>
  <done>The Learn index and per-post route render the seeded land-contract explainer, flagged as awaiting owner review and framed as general information rather than legal advice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical one-pager → rendered page | Legally-refined copy crossing from a source file into public HTML. The transcription step is where refined wording is most likely to be silently altered. |
| freshly authored copy → public HTML | The FAQ and the seeded Learn post have no upstream source, so nothing external constrains their claims except the review gate and the prohibitions recorded here. |
| content file → build | Blog frontmatter validated by Zod (ASVS V5). |
| Inquire link → contact page | A property slug carried in a query parameter. Phase 1 only receives it; Phase 3 reads it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-19 | Tampering | Land-contract copy paraphrased, "improved", or regenerated during porting into `how-it-works.astro` | high | mitigate | The task mandates character-for-character transcription from the canonical file and forbids rewriting. Verified mechanically: the "What a land contract means" paragraph is extracted from both the source file and the built page, whitespace-normalised, and required to match exactly. This is the phase's single highest-consequence content operation. |
| T-01-20 | Tampering | A retired phrasing reintroduced by fresh FAQ or Learn authoring | high | mitigate | The three retired phrasings are referenced by pointer to design spec section 6 and deliberately not repeated in this plan, so they cannot be reintroduced by being copied out of it. Asserted absent site-wide by three negative greps over `dist/`. |
| T-01-21 | Tampering | Compliance copy becoming CMS-editable | high | mitigate | The explainer lives in a `.astro` page, asserted by a grep requiring zero occurrences of `agreement for deed` anywhere under `src/content/`. A field that does not exist in a collection cannot be surfaced by Phase 2's CMS. |
| T-01-22 | Tampering | Authored copy promising approval, ownership, or credit | high | mitigate | Forbidden explicitly in both authoring tasks and carried as a flagged prohibition for judgment review. The footer's not-a-commitment-to-lend disclaimer is on every page; body copy contradicting it would be worse than silence. |
| T-01-23 | Repudiation | Unreviewed drafted copy read as owner-authored or attorney-reviewed guidance | medium | mitigate | `ownerReviewed: false` is a schema field, not a convention, and the rendered post carries a general-information-not-legal-advice note matching the canonical one-pager's own line. Owner review is a tracked Phase 4 gate. |
| T-01-24 | Information Disclosure | Property slug leaking through the Inquire query parameter | low | accept | The slug is already a public URL segment; passing it as a query parameter discloses nothing new. Phase 3 owns the handling once Zoho receives it. |
| T-01-25 | Spoofing | An external form or script embedded on the Contact page ahead of Phase 3 | low | mitigate | The task forbids embedding a form or external script, and the acceptance criteria assert the built Contact page contains neither. The slot is a marked comment only. |

**Canon referral (not minted as prohibitions):** cross-site scripting and injection are canon, owned
by `/gsd-secure-phase`; they are near-vacuous on hand-authored static pages with no runtime input.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 01-04 contribution)

**Routes:** `/` (completed), `/how-it-works`, `/about`, `/schedule`, `/contact`, `/learn`,
`/learn/<slug>` (from `src/pages/learn/[slug].astro`)

**Component names:** `StepList.astro`

**Content files:** `src/content/blog/what-is-a-land-contract.md`

**Marked extension point:** the Phase 3 Zoho web-to-lead embed slot in `src/pages/contact.astro`
(in addition to the site-wide integrations slot already in `Layout.astro`)

**Nav completeness:** with this plan all 7 nav destinations resolve — `/`, `/homes`,
`/how-it-works`, `/about`, `/learn`, `/schedule`, `/contact`
</artifacts_this_phase_produces>

<verification>
- All seven nav destinations build and resolve; every property Inquire link points at a page that exists.
- The "What a land contract means" paragraph on the built page is byte-identical, after whitespace normalisation, to the same paragraph in the canonical one-pager.
- None of the three retired phrasings appears anywhere under `dist/`.
- No legally-sensitive copy exists anywhere under `src/content/`.
- The D-07 featured fallback renders a non-empty section when no home is flagged featured.
- The Learn index and the seeded post both build, and the post is flagged `ownerReviewed: false`.
</verification>

<success_criteria>
1. A visitor can reach every page from the navigation and read the whole site end to end.
2. The land-contract wording on the site is the owner's refined wording, transcribed rather than rewritten.
3. EDU-02's FAQ exists as genuinely new copy grounded in that locked wording, not as a paraphrase of something that was never there.
4. The Learn section is real — an index plus a published post — and the drafted post is honestly marked as awaiting review.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done. Record the FAQ questions actually
authored, so the Phase 4 owner-review checklist can name them alongside the seeded Learn post.
</output>